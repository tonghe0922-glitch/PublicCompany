# P10-COMP-05 construction report

Status: `TASK_CLOSEOUT_CONSTRUCTION`; this report does not declare
P10-COMP-05, P008/P009/P010, L3, L5, or any L0–L5 layer PASS.

## 1. Contract, baseline and ownership

The independently accepted external `CONTRACT-PATCH-07` added only four exact
P10-COMP-05 paths: the L0–L5 coverage matrix and the P008, P009 and P010 live
E2E specs. It added no wildcard and changed no dependency, acceptance,
forbidden rule or other task. The external YAML, machine task, manifest and
checksum closure remain outside this workspace task and must be classified as
an authorized external contract patch.

The authoritative raw fresh-50 baseline is workspace
`60FC84F8D8FDB2F9773CE63D2A9E58B95F0CEFA48C2C8DC9C35ECFC8520B6C6C`
/1578 and task
`58E0D0DAED27BBB15EA49AD4715B28C751EF4BBA2F2AE6F2873EA42BE20EB9E5`
/50, with 10 existing and 40 missing task paths and metadata empty/0.
Package LOCAL/00 and LOCAL/01 were real exit-1 attempts at unavailable package
tooling; project preflight and planned baseline exited 0 and form the valid
baseline. Seven pre-existing API/worker/Vite runtime processes and listeners
5173–5175 were stable source-baseline non-writers. They are not task processes
and are never represented as process-zero Gate evidence.

## 2. Canonical plans and unmanufactured authority

The three canonical plans for P008, P009 and P010 validate against the machine
schema and use only public, ready registry component IDs. They remain distinct
process plans rather than one optional-field interface. Page-adjacent legacy
plans are not treated as authority.

P008 records the missing handover-person directory as
`BLOCKED_BY_CONTRACT`; the supported create path sends a null handover agent
and never accepts a free-text or fabricated UUID. P009 records that amount and
receipt reveal needs an authoritative session-level authorization chain and
therefore defaults those facts to a masked projection. P010 records both the
missing scoped learner directory and the missing sensitive reveal chain: it
exposes no UI or domain create channel and defaults score, practical and
qualification facts to masked output. No plan invents `allowedActions`, a
directory cache, step-up truth or a local reveal ticket.

## 3. P008 accepted subtask

P008 implements typed leave contracts, selectors, exact service URLs and
methods, independently scoped records/quota/creation/action resources, a
composable, four components, a process-local public index and a thin page.
Caller-created idempotency keys, AbortSignal, last-request-wins, 403/409,
status-kind contradictions and uncertain-result retry semantics have focused
tests. Record navigation and desktop/mobile projections share
`AsyncState.data`; quota state is independent. Validation errors start a new
create lifecycle only after the form actually changes and becomes valid.

The accepted focused matrix passed 4 files/69 tests, typecheck and lint exited
0, and exact P008 page source findings are 0. The final live spec is
`34432F10C0E35DBC723222BDB383EC0E2FE8455DF453771B7DB949152BFEE3C2`
/10627. Its unchanged legacy run reached the real page and failed the obsolete
heading selector. The adapted fresh desktop Chromium run passed 1/1 while
preserving all three portals, data-scope and 403 negatives, a real no-handover
create, quota and complete lifecycle facts, masking, and exact cleanup.

This is a P008 subtask closure only. The missing handover directory remains a
real block, so it is not a process or layer closure.

## 4. P009 accepted subtask

P009 implements its own complete overtime record/item/create/action
contracts, node/permission candidates, service, independent async resources,
composable, components, process index and thin page. The UI keeps server
records as its single fact source, scopes pending/error/key state per record,
uses latest-version refresh after conflict and masks amount and receipt facts
without a local reveal path. Typed action validation matches the backend
workflow rather than accepting a generic payload.

The accepted focused matrix passed 4 files/76 tests; typecheck, lint and exact
P009 page source checks passed. The final live spec is
`FE69AFB7CE5F059F503BC5421CDF82AAAE5C3180BF6AAEA74B73E1C5E7EA36A9`
/13295. Its retained adaptation history includes the same-origin proxy response
predicate correction, role/node visibility corrections and precise POST
synchronization corrections. Those failures were not hidden. The final fresh
desktop Chromium run passed 1/1 and retained three portals, scope/403/409
negatives, the full overtime-to-receipt lifecycle and masked UI projections.

This is a P009 subtask closure only. Sensitive reveal remains
`BLOCKED_BY_CONTRACT`, so it is not a process or layer closure.

## 5. P010 accepted subtask

P010 deliberately omits create from its public service and composable because
the backend requires an owner employee while no scoped authoritative person
directory exists. Its create component is only a visible blocked projection:
it has no input, option, submit event or hidden request path. The list/get/action
domain, ten-node permission selectors, independent async state, conflict
refresh, components, process index and thin page are implemented and tested.
Qualification, exam, practical and event evidence remain masked without a
session-authorized reveal contract.

The accepted focused matrix passed 4 files/70 tests; typecheck, lint and exact
P010 page source checks passed. The final live spec is
`617E89552942547D5335C55B4D721648A57D4D8660174A73A46672A9A5AF38A2`
/13559. The unchanged baseline spec produced a valid red failure on its old
page contract. The adapted spec proves UI create POST count zero and reads the
fresh fixture runtime learner ID only for an authorized manager API seed; the
ID is never inserted into UI or domain state and is not a directory truth.

The first green run retained a real exit-1 timeout: PUBLISH succeeded, then the
new S02 ActionPanel correctly disabled ASSIGN because the spec had not supplied
new-node evidence. Independent review classified this as an E2E input timing
defect and authorized exactly one evidence fill before ASSIGN. After that
mechanical correction, single-file ESLint and typecheck exited 0, and a new
fresh desktop Chromium run passed 1/1 (test 13.6 seconds, total 19.6 seconds).
PUBLISH, ASSIGN, COMPLETE_LEARNING and SUBMIT_EXAM each use their own evidence
and exact POST pathname synchronization. Practical assessment and certification
use independent actors; the same-assessor certification negative remains 409.

The final runtime used PostgreSQL container
`c8c25c4b8804afd939291c007e4c24ffbc44c7bf87ae13b5d259361a4c45c98d`
and Redis container
`895fd7d9bcd10c6fcc2355ea15e0e52414ce2764f3820508b532a32f382312f5`.
Only launcher PID 50728 was terminated with `/F /T`; both exact IDs are absent,
running Testcontainers/Ryuk is 0, listeners 18090/5330/5331/5332 are 0, new
fixture/web processes are 0 and the original runtime remains 7/7.

This is a P010 subtask closure only. Owner-directory and sensitive-reveal
contracts remain blocked, so it is not a process or layer closure.

## 6. Construction failure history

Tests preceded each domain and component/page implementation. The retained
history includes missing-module reds, fixture corrections, typed error and
idempotency failures, record double-truth checks, action/creation lifecycle
corrections, lint/type corrections, and each E2E red/green wrapper or assertion
failure. No production code was rolled back to manufacture a red test; no
Gate, lint rule, timeout, selector assertion or backend permission was weakened.

The P008 chain includes a formal UI checkpoint failure for creation-error
recovery and the subsequent 69/69 correction. The P009 chain retains its
resolver timeout fixture correction and four E2E role/timing adaptations before
the final pass. The P010 chain retains its page fallback correction, test lint
correction, valid legacy red, one first-green disabled-action timeout and the
independently approved one-line evidence fix.

## 7. Closeout debt and layer accounting

A read-only closeout wrapper first failed before any Gate result because three
PowerShell path/argument expressions omitted required spaces. Each child exited
2 and produced no JSON. That orchestration failure is retained and is not a
Gate result. One mechanical wrapper correction then produced the authoritative
current facts:

- project UI Gate: expected exit 1, 92 findings across 105 scanned sources,
  comprising 80 raw interactive elements and 12 legacy imports, with structural
  findings 0;
- source Gate: expected exit 1, 24 findings across seven route pages and 46
  action codes; P008, P009 and P010 exact page findings are all 0;
- external package phase10 Gate: exit 0, error 0 across 69 scanned files.

The package Gate reaching zero is only the result of that one scoped Gate. It
does not resolve missing directory or reveal contracts, close a process, close
a page, or establish a layer PASS. L2 remains 13/16 ready plus three blocked;
L3 remains 0/5 closed with P006–P010 as five partial implementations; L5
P006–P010 likewise remains 0/5 closed with five partial thin pages.

## 8. Construction closeout and next boundary

The first attempt to capture the Python fixture logs used the malformed
PowerShell expression `Join-Path$e`. The child commands completed, but the
wrapper did not create the declared logs; this is retained as an orchestration
failure and is not reported as authoritative evidence. A single mechanical
correction reran the same commands and wrote complete logs and exit files.

The authoritative construction Gate matrix is:

- Python compile exit 0; official self-test exit 0 with 85/85 cases;
- original reviewer fixture exit 0 with 6/6 probes and `fail_open_count=0`;
- resubmit-1 reviewer fixture exit 0 with 7/7 probes and
  `fail_open_count=0`;
- reference-context fixture exit 0 with 20/20 cases and no failure;
- project UI current expected exit 1 with 92 findings in 105 scanned files,
  split as 80 raw plus 12 legacy, structural findings 0;
- source self-test exit 0; source current expected exit 1 with 24 findings and
  P008/P009/P010 page findings each 0;
- external package self-test exit 0; authoritative external package phase10
  current exit 0 with 0 errors in 69 scanned files.

The authoritative construction frontend matrix is:

- all twelve P008/P009/P010 focused suites plus the public alias test: exit 0,
  13 files and 217 tests;
- single-file ESLint for the three live E2E specs: exit 0;
- full lint exit 0 and typecheck exit 0;
- full unit tests exit 0, 62 files and 536 tests;
- employee, center and admin builds all exit 0; the existing approximately
  2.12 MB chunk warning remains visible and no threshold was changed;
- Knip exit 0.

No fixture or Browser was started during this closeout construction matrix.
The 50-path SHA/bytes inventory and runtime boundary are frozen separately for
the requested final Browser/post window. Until that window is independently
authorized and reviewed, this remains a construction report,
P10-COMP-06+ remains locked, and no LAYER PASS is claimed.

## 9. Final frozen matrix and representative Browsers

The independently authorized final window first reproduced the frozen
fresh-50 boundary: 50 existing task paths, 40 added plus 10 modified paths,
and a report-excluded classification of 49 task changes, 0 unrelated, 0
concurrent and 1 metadata change. The original seven source-baseline runtime
processes and their listeners 5173/5174/5175 remained present and were never
stopped or reused.

The complete final Gate and frontend matrix was rerun rather than inherited:
Python compile, official 85, reviewer 6 and 7, and reference 20 all exited 0;
the project UI current remained the expected 92 findings in 105 files (80 raw
plus 12 legacy, structural 0); source self exited 0 and source current remained
24 with P008/P009/P010 each 0; external package self/current exited 0 with
0 errors in 69 files. Focused plus alias tests passed 13 files/217 tests, all
three live specs passed single-file ESLint, full lint and typecheck exited 0,
full unit passed 62 files/536 tests, all three builds exited 0 with the existing
chunk warning, and Knip exited 0.

The first formal P008 fixture command omitted the three required environment
values and ended before READY with `required environment missing:
PHASE10_P008_TENANT`. This wrapper failure is permanently retained and is not
a Browser or product result. After independent confirmation of the exact
fixture contract, one authorized corrected P008 launch explicitly supplied
the tenant, manager login and password. P009 and P010 likewise received their
own confirmed environment values from the outset; no fixture was reused.

The three representative desktop Chromium units then completed strictly in
order:

- P008: READY, 1/1 passed (15.5 seconds test, 21.4 seconds total). PostgreSQL
  `9ede925099787bc42bee4f17e2f637761f301960270ad7d70d7b7f601211e1ac`
  and Redis
  `ba4e55a007564824f962dd87732cba55fc65ad04d348c2655038bcb6a24afdc0`
  are absent; 18088/5316/5317/5318, Testcontainers, Ryuk and new processes
  are 0.
- P009: READY, 1/1 passed (14.3 seconds test, 20.2 seconds total). PostgreSQL
  `4f63f94eeb4a86beec5b97416eccc82642d1037f3d0602114cfa39a2feed1311`
  and Redis
  `64cab9399b6db132310712a22c150828b365b8daf20a36365a6c427286056469`
  are absent; 18089/5326/5327/5328, Testcontainers, Ryuk and new processes
  are 0.
- P010: READY, 1/1 passed (13.6 seconds test, 18.7 seconds total). PostgreSQL
  `dd59f204d33c7e467d5b445ae205df739d9c6096d185b520d088a1de9249e2d3`
  and Redis
  `4646551f3734f5a0327ee8352a58a71878425e8eb3f86927d0ff14694d7d6a1a`
  are absent; 18090/5330/5331/5332, Testcontainers, Ryuk and new processes
  are 0.

Each launcher was terminated only by its recorded PID after Playwright ended,
and exact cleanup completed before the next fixture began. The original
runtime remained 7/7 throughout. These results complete the submitted task
evidence but do not establish task or layer PASS; independent final review is
still required, P10-COMP-06+ remains locked, and L2/L3/L5 accounting in the
coverage matrix remains unchanged.
