# ABORTED

This snapshot was valid when generated, but it is not a formal resubmission
baseline. Independent review required the canonical plan schema itself to add
`source_facts` and `responsive_projection` to `required`. That allowed-path
change occurred after this snapshot, so workspace `5AC2C45F...13FFB1` and task
`1701FEBB...510F2` must not be reused as the formal pre or post baseline.
