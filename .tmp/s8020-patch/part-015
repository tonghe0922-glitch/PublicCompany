be('PHASE-08 identity switch failure boundary', () => {
   it('preserves the previous valid session when the switch command itself is rejected', async () => {
     const fetchFn: typeof fetch = (input) => {
       const path = typeof input === 'string' ? input : input instanceof URL ? input.href : input.url
-      if (path.endsWith('/auth/login')) return Promise.resolve(json(tokenResponse()))
+      if (path.endsWith('/auth/login')) {
+        return Promise.resolve(json({ ...tokenResponse(), session: sessionResponse() }))
+      }
       if (path.endsWith('/session/switch')) {
         return Promise.resolve(json({ status: 403, code: 'forbidden', detail: 'denied', requestId: 'req-switch' }, 403))
       }
diff --git a/technical-platform/web/src/shared/layout/rebuild/UnifiedPortalShell.test.ts b/technical-platform/web/src/shared/layout/rebuild/UnifiedPortalShell.test.ts
index 57841fa..a90d68d 100644
--- a/technical-platform/web/src/shared/layout/rebuild/UnifiedPortalShell.test.ts
+++ b/technical-platform/web/src/shared/layout/rebuild/UnifiedPortalShell.test.ts
@@ -17,7 +17,7 @@ async function mountShell() {
   await router.push('/tasks')
   const wrapper = mount(UnifiedPortalShell, {
     props: {
-      portal: PORTALS.employee,
+      portal: PORTALS.work,
       pageTitle: '待办与任务',
       searchItems: [{ sourceKey: 'tasks', label: '待办与任务', routePath: '/tasks' }],
     },
diff --git a/technical-platform/web/src/shared/layout/rebuild/UnifiedPortalShell.vue b/technical-platform/web/src/shared/layout/rebuild/UnifiedPortalShell.vue
index 68b9a84..8d02421 100644
--- a/technical-platform/web/src/shared/layout/rebuild/UnifiedPortalShell.vue
+++ b/technical-platform/web/src/shared/layout/rebuild/UnifiedPortalShell.vue
@@ -16,8 +16,8 @@ onMounted(()=>window.addEventListener('keydown',handleGlobalKeydown));onBeforeUn
   <div class="rebuild-shell" :class="{'rebuild-shell--has-alert':hasAlert,'rebuild-shell--sidebar-collapsed':sidebarCollapsed,'rebuild-shell--drawer-open':drawerOpen}">
     <a class="rebuild-shell__skip-link" :href="`#${contentId}`" @click="focusMain">跳到主要内容</a>
     <header class="rebuild-shell__topbar">
-      <button class="rebuild-shell__nav-toggle rebuild-shell__nav-toggle--desktop" type="button" aria-label="展开或收起主导航" :aria-expanded="(!sidebarCollapsed).toString()" @click="toggleSidebar"><svg viewBox="0 0 24 24"><path d="M4 6h16M4 12h16M4 18h16"/></svg></button>
-      <button class="rebuild-shell__nav-toggle rebuild-shell__nav-toggle--mobile" type="button" aria-label="打开主导航" :aria-expanded="drawerOpen.toString()" @click="toggleDrawer"><svg viewBox="0 0 24 24"><path d="M4 6h16M4 12h16M4 18h16"/></svg></button>
+      <button class="rebuild-shell__nav-toggle rebuild-shell__nav-toggle--desktop" type="button" aria-label="展开或收起主导航" :aria-expanded="!sidebarCollapsed" @click="toggleSidebar"><svg viewBox="0 0 24 24"><path d="M4 6h16M4 12h16M4 18h16"/></svg></button>
+      <button class="rebuild-shell__nav-toggle rebuild-shell__nav-toggle--mobile" type="button" aria-label="打开主导航" :aria-expanded="drawerOpen" @click="toggleDrawer"><svg viewBox="0 0 24 24"><path d="M4 6h16M4 12h16M4 18h16"/></svg></button>
       <RouterLink class="rebuild-shell__brand" to="/" aria-label="返回上金谷管理平台首页"><img class="rebuild-shell__brand-logo" :src="brandLogoUrl" alt="上金谷品牌标志"/><span class="rebuild-shell__brand-copy"><span class="rebuild-shell__brand-title-row"><strong>上金谷管理平台</strong><span class="rebuild-shell__brand-tag">{{ portalTag }}</span></span><small>数字化现场调度协同系统</small></span></RouterLink>
       <div class="rebuild-shell__search" :class="{'is-open':searchOpen}"><svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"/><path d="m20 20-3.5-3.5"/></svg><input ref="searchInputRef" v-model="query" type="search" autocomplete="off" aria-label="搜索当前可访问页面" placeholder="搜索当前可访问页面" data-test="shell-search" @focus="searchOpen=true"/><kbd>Ctrl K</kbd><div v-if="searchOpen" class="rebuild-shell__search-results" role="listbox"><button v-for="item in searchResults" :key="item.sourceKey" type="button" role="option" @click="navigate(item.routePath)"><span>{{ item.label }}</span><small>{{ item.routePath }}</small></button><p v-if="!searchResults.length">没有匹配的已开放页面</p></div></div>
       <div class="rebuild-shell__topbar-actions"><button class="rebuild-shell__search-trigger" type="button" aria-label="搜索页面" @click="openSearch"><svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"/><path d="m20 20-3.5-3.5"/></svg></button><slot name="header"/></div>
diff --git a/technical-platform/web/admin.html b/technical-platform/web/tech.html
similarity index 80%
rename from technical-platform/web/admin.html
rename to technical-platform/web/tech.html
index 6dcd282..6042c7d 100644
--- a/technical-platform/web/admin.html
+++ b/technical-platform/web/tech.html
@@ -9,9 +9,9 @@
   <div id="app"></div>
   <script>
     if (location.protocol === 'file:') {
-      document.getElementById('app').innerHTML = '<main style="max-width:760px;margin:80px auto;padding:32px;font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Microsoft YaHei,sans-serif;border:1px solid #e5e7eb;border-radius:20px;background:#fff;box-shadow:0 12px 40px rgba(15,23,42,.08)"><h1 style="margin:0 0 14px;color:#0f172a;font-size:26px">请通过项目启动脚本打开技术端</h1><p style="color:#475569;line-height:1.8">本页面是 Vue/Vite 源码入口，不能通过 file:// 直接双击运行。请返回项目根目录运行 <b>启动技术端.bat</b>，或在 technical-platform/web 执行 <code>pnpm dev:admin</code>，然后访问 <code>http://localhost:5175/admin.html</code>。</p></main>';
+      document.getElementById('app').innerHTML = '<main style="max-width:760px;margin:80px auto;padding:32px;font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Microsoft YaHei,sans-serif;border:1px solid #e5e7eb;border-radius:20px;background:#fff;box-shadow:0 12px 40px rgba(15,23,42,.08)"><h1 style="margin:0 0 14px;color:#0f172a;font-size:26px">请通过项目启动脚本打开技术端</h1><p style="color:#475569;line-height:1.8">本页面是 Vue/Vite 源码入口，不能通过 file:// 直接双击运行。请返回项目根目录运行 <b>启动技术端.bat</b>，或在 technical-platform/web 执行 <code>pnpm dev:tech</code>，然后访问 <code>http://localhost:5175/tech.html</code>。</p></main>';
     }
   </script>
-  <script type="module" src="/src/portals/admin/main.ts"></script>
+  <script type="module" src="/src/portals/tech/main.ts"></script>
 </body>
 </html>
diff --git a/technical-platform/web/vite.config.ts b/technical-platform/web/vite.config.ts
index dd06c80..ff6e83a 100644
--- a/technical-platform/web/vite.config.ts
+++ b/technical-platform/web/vite.config.ts
@@ -3,27 +3,85 @@ import { defineConfig } from 'vite'
 import vue from '@vitejs/plugin-vue'
 
 const basePort = Number(process.env.SJG_PORT_BASE ?? 5173)
+const localApiProxyTarget = process.env.SJG_LOCAL_API_PROXY_TARGET ?? 'http://127.0.0.1:8080'
 const portalBuilds = {
-  work: { input: 'work.html', outDir: 'dist/work', port: basePort },
-  admin: { input: 'admin.html', outDir: 'dist/admin', port: Number(process.env.ADMIN_WEB_PORT ?? 5175) },
+  work: {
+    input: 'work.html',
+    outDir: 'dist/work',
+    port: basePort,
+  },
+  tech: {
+    input: 'tech.html',
+    outDir: 'dist/tech',
+    port: Number(process.env.TECH_WEB_PORT ?? 5175),
+  },
 } as const
-const localApiProxyTarget = process.env.SJG_LOCAL_API_PROXY_TARGET ?? 'http://127.0.0.1:8080'
+
 type PortalMode = keyof typeof portalBuilds
-function isPortalMode(mode: string): mode is PortalMode { return mode in portalBuilds }
+
+function isPortalMode(mode: string): mode is PortalMode {
+  return mode in portalBuilds
+}
+
 function vueClientTestPlugin() {
-  const plugin = vue(); const originalLoad = plugin.load; const originalTransform = plugin.transform
-  if (!originalLoad || typeof originalLoad !== 'object' || typeof originalLoad.handler !== 'function' || !originalTransform || typeof originalTransform !== 'object' || typeof originalTransform.handler !== 'function') throw new Error('Unsupported @vitejs/plugin-vue hook shape for runtime tests')
-  const loadHandler = originalLoad.handler; const transformHandler = originalTransform.handler
-  plugin.load = {...originalLoad, handler(id, options) { return loadHandler.call(this, id, { ...(options ?? {}), ssr: false }) }}
-  plugin.transform = {...originalTransform, handler(code, id, options) { const clientOptions = options ? { ...options, ssr: false } : undefined; return transformHandler.call(this, code, id, clientOptions) }}
+  const plugin = vue()
+  const originalLoad = plugin.load
+  const originalTransform = plugin.transform
+  if (
+    !originalLoad
+    || typeof originalLoad !== 'object'
+    || typeof originalLoad.handler !== 'function'
+    || !originalTransform
+    || typeof originalTransform !== 'object'
+    || typeof originalTransform.handler !== 'function'
+  ) {
+    throw new Error('Unsupported @vitejs/plugin-vue hook shape for runtime tests')
+  }
+  const loadHandler = originalLoad.handler
+  const transformHandler = originalTransform.handler
+  plugin.load = {
+    ...originalLoad,
+    handler(id, options) {
+      return loadHandler.call(this, id, { ...(options ?? {}), ssr: false })
+    },
+  }
+  plugin.transform = {
+    ...originalTransform,
+    handler(code, id, options) {
+      const clientOptions = options ? { ...options, ssr: false } : undefined
+      return transformHandler.call(this, code, id, clientOptions)
+    },
+  }
   return plugin
 }
-function portalServer(port: number) { return {port, proxy: {'/api': {target: localApiProxyTarget, changeOrigin: true}}} }
-function analysisConfig() { return { plugins: [vue()] } }
+
+function portalServer(port: number) {
+  return {
+    port,
+    proxy: {
+      '/api': {
+        target: localApiProxyTarget,
+        changeOrigin: true,
+      },
+    },
+  }
+}
+
 export default defineConfig(({ mode }) => {
-  if (mode === 'test') return {plugins: [vueClientTestPlugin()]}
-  if (mode === 'development' || mode === 'production') return analysisConfig()
+  if (mode === 'test') return { plugins: [vueClientTestPlugin()] }
+  if (mode === 'development' || mode === 'production') return { plugins: [vue()] }
   if (!isPortalMode(mode)) throw new Error(`Unsupported portal mode: ${mode}`)
   const portal = portalBuilds[mode]
-  return {base: './', plugins: [vue()], server: portalServer(portal.port), build: {outDir: portal.outDir, emptyOutDir: true, rollupOptions: {input: resolve(import.meta.dirname, portal.input)}}}
+  return {
+    base: './',
+    plugins: [vue()],
+    server: portalServer(portal.port),
+    build: {
+      outDir: portal.outDir,
+      emptyOutDir: true,
+      rollupOptions: {
+        input: resolve(import.meta.dirname, portal.input),
+      },
+    },
+  }
 })
