---
project: 上金谷景区一体化运营管理平台
document: AI 编程代理工程执行宪法
filename: AGENT.md
version: V1.2
baseline_date: 2026-08-07
quality_gate_policy: quantitative + CI-enforced + ratchet
stack_decision: Vue 3.5 + TypeScript 5.9 + Pinia + Vue Router / Java 21 + Spring Boot 3.5 + Spring MVC + Spring JDBC / PostgreSQL 16 + Flyway
build_tool_policy: Vite supported release; Vite 5.4 is migration-only and not a long-term production baseline
status: 全仓库最高工程执行约束
canonical: true
canonical_source: /AGENT.md
knowledge_base_agent_policy: pointer-only; Knowledge Base copy MUST NOT override root AGENT
design_baseline: DESIGN.md V2.0
portal_canonical_codes: employee, center, tech
portal_runtime_aliases: employee=employee; center=center; tech=admin
language: zh-CN
scope: employee-web, center-web, tech-web, api, worker, database, integrations, tests, deployment
process_count: 126
endpoint_count: 3
form_count: 2164
source_field_count: 90124
database_baseline: PostgreSQL 16
core_database_count: 3
physical_schema_count: 46
physical_table_count: 265
---

# 上金谷景区一体化运营管理平台
# AI 编程代理工程执行宪法（AGENT.md）

> 本文件适用于 Codex、Claude Code、Cursor、GitHub Copilot、Cline、Roo Code、Stitch、通用代码智能体以及人工研发团队。  
> 本文件不是产品宣传稿，也不是简单的代码风格说明，而是将业务流程、三端表单、数据库架构、权限、安全、测试和上线规则转换为可执行的工程合同。  
> 任何智能体进入仓库后，必须先读取本文件，再读取相关需求、流程、数据库和设计基线；未完成这些步骤不得开始批量编码。

---

##  代码评审增补说明

本版本在业务架构、技术栈、数据库、安全和流程红线基础上，吸收代码质量评审结论，重点补齐“规则已经写了，但没有形成自动门禁”的问题。

本次增补遵循以下原则：

1. **不把当前代码当作正确标准。** 当前实现与本文件冲突时，修复代码、测试和工程配置，不削弱规则迁就旧实现。
2. **不进行大爆炸式重写。** 对巨型组件、三端复制和重复请求逻辑采用特征测试保护下的渐进式重构，始终保持仓库可编译、可启动、可回退。
3. **把主观要求变成量化门禁。** 对重复率、文件与函数复杂度、空异常、死代码、类型、测试和幂等增加可由 CI 自动验证的阈值。
4. **共享能力不等于抹平三端差异。** 共享无业务语义组件、契约、请求状态和通用特性；端侧布局、工作目标、数据范围和动作权限仍分别设计。


### 阶段一规范与知识统一结论（V1.2）

本版本正式完成平台级规范源和三端工程命名统一：

1. 根 `/AGENT.md` 为唯一 canonical 工程执行宪法；
2. 根 `/DESIGN.md` V2.0 为三端统一设计与 Vue 实现基线；
3. Knowledge Base 中的 AGENT 只能作为根 AGENT 的指针，不再维护第二份完整平台规则；
4. 前端正式技术路线固定为 Vue 3.5.x + TypeScript 5.9.x + Vite 8.2.x + Pinia + Vue Router；
5. 现有样式体系采用 CSS + Design Tokens，不引入 Tailwind 作为第二套样式技术栈；
6. 三端 canonical code 固定为 `employee / center / tech`，其中当前 runtime alias `tech = admin`；
7. 技术后台端不是业务超级管理员；
8. 页面架构必须从 Knowledge Base 六份三端页面 Excel 建立 route、permission、process 和 data scope 追溯。
---

## 0. 规范词与适用边界

### 0.1 规范词

- **必须（MUST）**：不满足即视为功能未完成或存在不可接受风险。
- **禁止（MUST NOT）**：任何实现、脚本、迁移、配置和临时修复均不得违反。
- **应当（SHOULD）**：原则上必须执行；确需偏离时必须形成 ADR，说明原因、风险、期限和回收计划。
- **可以（MAY）**：增强项，可根据阶段、成本和风险决定。

### 0.2 适用范围

本文件覆盖：

1. 员工端；
2. 中心管理端；
3. 技术后台端；
4. API、领域服务、流程引擎和异步任务；
5. PostgreSQL、Redis、对象存储、消息总线、搜索和分析库；
6. 数据迁移、初始化、备份、恢复和归档；
7. 自动化测试、安全测试、性能测试、部署和运维；
8. 所有 126 个业务闭环及其后续版本。

### 0.3 层级关系

- 本文件是**平台级**规则。
- 子目录可以存在更严格的 `AGENT.md`，但只能增加约束，不能削弱本文件。
- 若仓库内存在多个同名规则文件，离目标代码最近的文件负责模块细节，但平台级安全、数据、流程和审计红线不可被覆盖。


### 0.4 Canonical 规范源与 Knowledge Base 镜像规则

- 仓库根目录 `/AGENT.md` 是唯一 canonical 平台级工程宪法。
- `Knowledge Base/04 Agents开发规范/AGENT.md` 从 V1.2 起只能作为**指针文件**，负责提醒智能体读取根 `AGENT.md`，不得再保存一份可独立演进的完整副本。
- 禁止把 Knowledge Base 中的 AGENT 文件覆盖回仓库根目录。
- 子模块若存在 `AGENT.md`，只能对本模块增加更严格规则，并明确 `inherits: /AGENT.md`；不得重新声明另一套技术栈、数据安全口径、三端职责或流程事实源。
- 根 `AGENT.md` 发生变更时，只允许同步 Knowledge Base 指针文件中的 `canonical_version`、`canonical_baseline_date`、`canonical_source` 等指针元数据。
- 根 `DESIGN.md` V2.0 及后续版本是三端视觉、交互和前端实现的统一设计基线；旧设计稿中的 React、Tailwind、`lucide-react`、`App.tsx` 等描述仅可作为历史视觉参考，不得作为编码依据。
- 若子目录规则与根 `AGENT.md` 冲突，平台级安全、权限、数据库、流程、审计、技术栈和质量门禁以根 `AGENT.md` 为准。
---

## 1. 不可误解的项目结论

1. **这是可真实运行的企业运营平台，不是静态演示站。**
2. **员工端、中心管理端、技术后台端是同一业务事实的不同权限视图，不是三套独立系统和三套重复数据。**
3. **技术后台端不是“超级业务管理员”。** 技术角色负责配置、运行、监控、补偿和审计，不自动获得业务审批权、薪资知悉权或敏感数据阅读权。
4. **所有业务必须闭环。** 发起、校验、审批、执行、验收、影响回写、通知、归档、异常和补偿缺一不可。
5. **点击按钮、发送通知、上传附件、创建工单、扫码签到、生成待办都不等于业务完成。**
6. **业务状态只能由服务端状态机和领域命令改变。** 前端不得提交任意目标状态，技术后台不得直接改库伪造完成。
7. **稳定业务字段进入领域主表；重复项进入明细分表；动态低频字段进入受控动态值层；附件进入对象存储。**
8. **所有涉及金额、库存、积分、票务、签署、结算、权限和审计的事实必须可复算、可追踪、可冲销，不得覆盖历史。**
9. **制度中的金额、比例、时限、等级、分数、预警和法律参数必须配置化、版本化、带生效范围，不得散落硬编码。**
10. **系统可以给出建议、风险提示和材料汇总，但不得自动替代必要的人事、财务、法律、安全和专业判断。**

---

## 2. 权威资料、冲突处理与追溯

### 2.1 必须检索的基线资料

智能体在实现任何流程前，必须优先寻找并读取以下资料的最新已批准版本：

1. `AGENT.md`；
2. 经确认的 PRD/SRS；
3. `S0 全部业务流程简表.xlsx`；
4. `上金谷三端业务流程表单字段包_V1.0_完整交付包.zip` 或其解压目录；
5. `上金谷运营管理平台_数据库架构设计包_V1.0_完整交付包.zip` 或其解压目录；
6. 数据库包中的：
   - `02_数据字典/数据库模型_machine_readable.json`；
   - `02_数据字典/03_全量表清单.csv`；
   - `02_数据字典/04_全量字段字典.csv`；
   - `02_数据字典/07_流程落表映射.csv`；
   - `04_初始化数据/01_process_catalog.csv`；
   - `04_初始化数据/02_state_catalog.csv`；
   - `04_初始化数据/03_business_rule_catalog.csv`；
   - `04_初始化数据/04_interface_catalog.csv`；
   - `04_初始化数据/05_three_endpoint_linkage.csv`；
   - `03_SQL_DDL` 下对应 SQL；
7. 最新 `DESIGN.md`、设计令牌、页面信息架构和品牌资源，其中页面 IA 以 `Knowledge Base/01 完整的页面架构` 下员工端、中心端、技术端六份 Excel 及其统一映射文件为准；
8. 当前代码、迁移、OpenAPI、自动化测试、ADR 和发布记录；
9. 最新代码质量评审、重复率/复杂度/死代码报告和 CI 质量基线。

### 2.2 信息优先级

发生冲突时按以下顺序处理：

1. 现行法律法规、监管要求和不可降低的安全底线；
2. 用户或业务方最新、明确、已确认的决定；
3. 本 `AGENT.md`；
4. 最新已批准 PRD/SRS、验收记录和 ADR；
5. 126 流程清单、三端字段包、状态/规则/接口目录；
6. 数据库架构包、数据字典、DDL 和流程落表映射；
7. 最新 `DESIGN.md` 和页面信息架构；
8. 当前可运行代码及通过的自动化测试；
9. 旧版本文档和历史原型；
10. 智能体自己的经验、偏好和猜测。

### 2.3 冲突处理规则

- 不得因为旧文件更详细就覆盖新的明确决定。
- 不得因为代码当前如此就认定代码一定正确；代码与权威基线冲突时，应修复代码和测试。
- 不得静默选择冲突的一方。
- 可逆、低风险歧义：采用最小权限、最少数据、最小改动、可回滚方案，并写入 `docs/assumptions.md`。
- 不可逆或高风险歧义：涉及正式合同、印章、薪资、支付、库存、票务核销、重大权限、数据删除、法律责任时，必须停止该高风险步骤并提出一个精准问题。
- 每个业务实现、迁移、测试和 PR 描述必须带 `process_code`；无法对应流程时，先判断是平台基础能力、缺失需求还是越界开发。

### 2.4 禁止自行发明

智能体禁止自行发明：

- 新的业务状态或状态含义；
- 新的数据库表、字段、枚举和唯一键口径；
- 新的审批人、权限范围、等级、金额、时限和自动决定规则；
- 新的组织名称、岗位名称和数据所有者；
- 新的事实源；
- 与既有流程重复的平行功能。

确需新增时，必须先提交变更影响矩阵、数据迁移方案和 ADR，再实施。

---

## 3. 三端职责和统一事实模型

### 3.1 员工端

员工端承担：

- 本人发起、填报、补充、执行、确认、回执、撤回、申诉和进度查看；
- 只查看本人、本人授权事项和当前职责所需数据；
- 提交真实执行事实和证据，不自行替代审批、专业复核和独立验收；
- 移动优先、步骤清晰、下一步明确、错误可修复。

### 3.2 中心管理端

中心管理端承担：

- 受理、完整性审查、业务真实性审核、资源协调、分派、专业复核、验收、结案、复盘和整改；
- 明确唯一主责人、协作人、验收人和 SLA；
- 不能替代一线执行人伪造现场事实；
- 不能以管理权限绕过回避、四眼复核和专业资质。

### 3.3 技术后台端

技术后台端承担：

- 流程定义、表单 Schema、规则、SLA、权限、接口、监控、审计、重试、死信和技术补偿；
- 环境、服务、数据库、消息、对象存储、任务和集成健康管理；
- 只执行经过授权的技术恢复和补偿，不直接作出业务批准或责任结论；
- 技术修复不得抹除已经发生的业务事实。

### 3.4 三端统一规则

- 三端共享同一个 `business_id`、`business_no`、`process_instance_id`、流程版本和状态事实。
- 三端的差异通过路由、动作权限、数据范围和字段可见性实现，不通过复制表或复制记录实现。
- 每个节点都必须说明三端职责、输入、输出、状态事件和关闭门槛。
- 刷新、跨设备、跨端和重复提交后，三端显示必须与服务端最终事实一致。


### 3.5 三端唯一命名口径

| Canonical Code | 中文名称 | 当前运行/构建别名 | 当前代码目录 | 业务职责 |
|---|---|---|---|---|
| `employee` | 员工端 | `employee` | `technical-platform/web/src/portals/employee` | 本人发起、执行、补充、确认、查询 |
| `center` | 中心管理端 | `center` | `technical-platform/web/src/portals/center` | 受理、审核、分派、专业复核、验收 |
| `tech` | 技术后台端 | `admin` | `technical-platform/web/src/portals/admin` | 配置、监控、重试、补偿、审计 |

强制规则：

- `admin` 是当前工程的 runtime/build alias，不代表第四个门户，也不代表“超级业务管理员”。
- Knowledge Base、PRD/SRS、测试矩阵、业务文档和机器目录统一使用 `employee / center / tech` 作为 canonical 端口编码。
- 当前阶段不进行无收益的大规模 `admin → tech` 路径改名；若未来正式修改运行别名，必须通过 ADR，并同步 Vite 构建、部署、域名、监控、E2E、运维和回滚方案。
- 页面 IA 必须以 `Knowledge Base/01 完整的页面架构` 六份 Excel 为业务来源，并通过 `portal_code + route + permission_code + process_code + data_scope` 建立可追溯映射。
- 三端页面可以不同，但同一业务必须共享同一个 `business_id`、`business_no`、`process_instance_id` 和服务端状态事实。
- 技术后台只拥有经授权的技术治理能力，不因 `admin` 命名自动获得业务审批权、薪资知悉权、付款决定权、合同决定权或敏感数据明文访问权。
---

## 4. 总体架构决策

### 4.1 首期架构

首期采用：

- **模块化单体**；
- **清晰领域边界**；
- **领域事件**；
- **Transactional Outbox**；
- **独立异步 Worker**；
- **三端前端应用共享设计系统和契约**；
- **PostgreSQL 16 为唯一交易事实源**。

禁止为了“看起来先进”提前拆成大量微服务。只有在业务边界稳定、独立扩缩容、独立安全域、团队边界或容量达到拆分阈值时，才可通过 ADR 拆分。

### 4.2 数据拓扑

- `sjg_oms`：核心交易、主数据、流程和业务事实；
- `sjg_audit`：操作、访问、变更、规则、导出和安全审计，只增不改；
- `sjg_dw`：维度、事实、指标和报表，只由 CDC/ETL 写入；
- Redis：会话、验证码、权限缓存、锁、限流和短期任务状态，不是事实源；
- S3/MinIO：附件、合同、签署证据、照片、导出和原始文件；
- RabbitMQ 或 Kafka：领域事件、异步任务、补偿和通知；一个环境只选择一套主消息总线，禁止无理由并存；
- OpenSearch：可选全文检索和日志检索，不作为审批事实源。

### 4.3 事务边界

- 单一聚合根：本地 ACID 事务。
- 同库跨 Schema 查询：可做受控只读查询；写入必须调用数据所属领域服务。
- 跨领域副作用：业务事务内写领域事实、流程动作和 Outbox；事务提交后异步消费。
- 跨数据库或外部系统：Outbox + Inbox/幂等消费者 + Saga/补偿，不使用分布式强事务伪装可靠性。
- 审计和分析写入失败时，按风险等级决定阻断或进入补偿队列；关键审计失败必须阻断高风险动作。

---

## 5. 已批准技术栈与版本策略

### 5.1 技术栈决策（MUST）

本项目已存在明确、合理且可持续演进的技术路线。除本节明确允许的版本升级外，智能体和研发人员必须沿用，不得擅自替换为 React、Next.js、NestJS、Node.js 后端、JPA/Hibernate、MySQL、MongoDB 或微服务体系。

| 层级 | 批准技术 | 强制规则 |
|---|---|---|
| 前端框架 | Vue 3.5.x | 使用 Composition API 与 `<script setup lang="ts">`；不得混入第二套前端框架 |
| 前端语言 | TypeScript 5.9.x | `strict: true`；禁止隐式 `any`、滥用类型断言和关闭类型检查 |
| 构建工具 | Vite | 新开发必须使用 Vite 官方仍在支持的稳定版本；截至 2026-08-07 的目标基线为 Vite 8.2.x |
| 状态管理 | Pinia | 按业务域拆分 Store；只保存跨页面客户端状态，不复制服务端业务事实 |
| 路由 | Vue Router 4.x | 路由元数据只描述访问要求；最终权限由后端再次校验 |
| 样式体系 | 现有 CSS + Design Tokens | 继承 `technical-platform/web/src/styles.css` 与 `technical-platform/web/src/styles/**`；禁止引入 Tailwind 作为平行样式体系 |
| 图标 | 现有 Lucide 依赖与适配方式 | 不使用 `lucide-react`；新增图标沿用当前 Vue 工程适配方式，不为单个图标引入第二图标库 |
| 后端语言 | Java 21 LTS | 编译和生产运行统一 Java 21；禁止开发、测试、生产使用不同主版本 |
| 后端框架 | Spring Boot 3.5.x | 固定 3.5 主次版本并使用最新安全补丁；未经 ADR 不升级 Spring Boot 4.x |
| Web 接口 | Spring MVC REST API | 使用 Servlet 栈；不得同时引入 WebFlux 形成两套请求模型 |
| 数据访问 | Spring JDBC / JdbcTemplate | 不使用 JPA/Hibernate；复杂参数优先 `NamedParameterJdbcTemplate` |
| 数据库 | PostgreSQL 16 | 使用当前 16.x 安全补丁；PostgreSQL 是唯一交易事实源 |
| 数据迁移 | Flyway | 所有 DDL、约束、索引、种子变更都必须通过版本迁移执行 |
| 架构 | 前后端分离 + 模块化单体 | 后端按领域模块隔离，使用领域事件和 Transactional Outbox，禁止过早微服务化 |
| 部署 | Docker | 后端生产镜像运行于 Java 21 JRE；前端构建产物使用静态服务器或网关托管 |

### 5.2 与原技术栈的冲突检查结论

1. **Vue 3.5、TypeScript 5.9、Pinia、Vue Router之间无架构冲突。**
2. **Java 21与Spring Boot 3.5兼容。** Spring Boot 3.5最低要求Java 17，Java 21属于受支持范围。
3. **Spring MVC与模块化单体匹配。** 本平台是事务型管理系统，不需要为了“异步”强行改成全链路响应式WebFlux。
4. **Spring JDBC/JdbcTemplate合理。** 本项目数据库模型复杂、PostgreSQL能力使用较深，显式SQL有利于审计、性能控制、RLS、分区、CTE和批量操作；但必须通过Repository封装，禁止SQL散落在Controller和Service。
5. **PostgreSQL 16与Flyway匹配。** Flyway必须显式启用PostgreSQL数据库支持模块，并由Spring Boot依赖管理或统一版本目录控制。
6. **Docker + Java 21 JRE合理。** 构建镜像和运行镜像分离，生产镜像不得携带源码、Maven缓存、测试工具和开发密钥。
7. **唯一不合理的是长期冻结在Vite 5.4。** Vite 5.4已不在官方支持版本范围，不再获得安全修复；它只允许作为现有项目的临时迁移起点，不得作为新功能和长期生产发布基线。

### 5.3 Vite 迁移规则

- 现有Vite 5.4项目不得直接删除重建；应保留业务代码和配置，按官方迁移指南逐级完成 `5.4 → 6.4 → 7.3 → 8.2` 兼容性验证。
- 每一级升级必须通过：安装、类型检查、单元测试、构建、关键E2E、静态资源路径、代理、环境变量、Sass/PostCSS和浏览器兼容验证。
- Vite升级不能与大规模业务重构合并为一个不可审查提交。
- 前端构建Node版本必须满足目标Vite要求；新基线使用Node 22 LTS且不低于22.12。
- 迁移期间若生产必须继续使用Vite 5.4，必须形成限期ADR，记录风险、隔离措施、迁移负责人和完成日期。

### 5.4 Spring Boot 版本规则

- Java固定为21。
- Spring Boot固定为3.5.x，并跟随3.5系列最新补丁；截至2026-08-07本项目工程基线继续锁定3.5.x；具体补丁版本以当前构建文件与官方受支持版本核验结果为准。
- Spring Framework、Jackson、Tomcat、Spring Security、Flyway等版本优先由Spring Boot BOM管理，禁止随意覆盖。
- 只有存在明确兼容性、安全或功能理由时才可覆盖BOM版本，并提供测试证据。
- 不得为了追新自动升级Spring Boot 4.x；升级必须独立立项并完成Jakarta、依赖、序列化、测试、监控和部署兼容评审。

### 5.5 先继承现有项目

开始编码前必须检查：

- `package.json`、包管理器和锁文件；
- `pom.xml`/`build.gradle`及Wrapper；
- Vue、TypeScript、Vite、Pinia、Vue Router实际版本；
- Java、Spring Boot、Spring Framework和数据库驱动实际版本；
- JdbcTemplate封装、事务边界、SQL组织和RowMapper方式；
- Flyway目录、版本命名、Schema历史和失败迁移状态；
- 登录、会话、权限、RLS和审计方式；
- 测试、Lint、格式化、构建和Docker命令；
- 当前模块边界、目录结构、代码风格和变更记录。

**不得因为智能体更熟悉另一套技术而重写已可运行项目，不得在同一端混入第二套前端框架、第二套Web栈或第二套ORM。**

### 5.6 禁止的技术捷径

- 禁止引入JPA/Hibernate并与JdbcTemplate并行维护同一领域数据。
- 禁止引入WebFlux并与Spring MVC混用同一业务接口。
- 禁止使用SQLite或H2替代PostgreSQL完成集成兼容验证。
- 禁止启动时自动建表、删表、重建Schema或绕过Flyway。
- 禁止在前端、脚本或浏览器内直接连接数据库。
- 禁止将Redis、浏览器存储、Excel或消息队列作为长期唯一事实源。
- 禁止无锁文件安装、依赖版本漂移或在同一提交无理由升级大量依赖。
- 禁止生产环境使用测试验证码、默认密码、固定Token、模拟签章和模拟支付。

---

## 6. 推荐仓库结构

已有仓库结构优先保留；只有在目录尚未形成稳定约定时，才采用下列基线。禁止仅为“看起来整齐”进行全仓库无功能收益搬迁。

```text
frontend/
  apps/
    employee-web/                 # 员工端，移动优先 Vue SPA
    center-web/                   # 中心管理端，桌面优先 Vue SPA
    tech-web/                     # 技术后台端，桌面优先 Vue SPA
  packages/
    ui/                           # 无业务语义的Vue组件与设计令牌
    contracts/                    # OpenAPI生成类型、稳定错误码、事件类型
    auth/                         # 路由守卫、会话、字段权限展示辅助
    forms/                        # 表单Schema渲染与校验基础能力
    testing/                      # Vitest、Vue Test Utils、Playwright辅助
  package.json
  pnpm-lock.yaml 或现有锁文件

backend/
  pom.xml 或 build.gradle         # 继承现有构建工具；空仓库默认Maven Wrapper
  src/main/java/<base-package>/
    shared/
      config/
      security/
      web/
      observability/
      outbox/
    modules/
      core/
      iam/
      org/
      mdm/
      workflow/
      document/
      notification/
      integration/
      audit/
      <business-domain>/
        api/                      # Controller、Request/Response DTO
        application/              # Use Case、事务、权限、编排
        domain/                   # 聚合、值对象、领域服务、事件
        infrastructure/           # JdbcTemplate Repository、外部Adapter
  src/main/resources/
    application.yml
    db/migration/                 # Flyway V/R迁移
    sql/                          # 可复用且版本受控的查询片段（如项目采用）
  src/test/java/
    unit/
    integration/
    architecture/

 database/
  baseline/                       # 已批准数据库基线，只读参考
  verification/                   # RLS、约束、字段和映射校验SQL
  seed-reference/                 # 初始化数据参考，不绕过Flyway

docs/
  requirements/
  architecture/
  adr/
  api/
  events/
  data-dictionary/
  permissions/
  test-matrix/
  runbooks/
  assumptions.md

scripts/
  install/
  start/
  verify/
  backup/
  restore/

docker/
  frontend/
  backend/
  compose/

.env.example
README.md
CHANGELOG.md
AGENT.md
```

规则：

- 前端按应用和共享包组织；禁止复制三套相同组件、权限守卫和API类型。
- 后端按领域模块组织；禁止全仓库横向堆积巨型 `controller/ service/ mapper/ utils/` 目录。
- 每个后端领域模块必须拥有清晰的API、应用、领域和基础设施边界。
- `JdbcTemplate`、事务、外部接口和Spring注解只能出现在允许的层次，领域层不得依赖Spring。
- `utils` 只能放无业务语义的小型纯函数，禁止成为杂物箱。
- OpenAPI生成代码与手写代码分目录，禁止手改会被重新生成覆盖的文件。
- 前后端可以位于同一仓库，也可以是现有的两个仓库；不得为满足目录示例强制合仓或拆仓。

---

## 7. 领域边界和数据所有权

### 7.1 平台核心域

以下域必须先于大多数业务模块稳定：

- `core`：租户、字典、参数、序列、幂等、Outbox；
- `iam`：账户、身份、多岗位切换、角色、权限、数据范围；
- `org`：中心、部门、岗位、员工、任职关系；
- `mdm`：客户、供应商、商户、产品、资产、位置等主数据；
- `workflow`：定义、版本、节点、实例、任务、动作、表单、规则、SLA、编排；
- `document`：文件对象、附件关联、文档、电子签署；
- `notification`：模板、消息、渠道、送达和签收；
- `integration`：接口、调用、重试、死信、导入任务；
- `audit`：导出、数据质量、安全事件和审计投递。

### 7.2 业务域

业务域包括但不限于：

`administration`、`archive`、`asset`、`attendance`、`brand`、`catering`、`cleaning`、`collaboration`、`content`、`costume`、`crm`、`devops`、`dormitory`、`drone`、`ecommerce`、`education`、`entertainment`、`finance`、`fleet`、`hr`、`itops`、`learning`、`maintenance`、`marketing`、`media`、`merchant`、`performance`、`planning`、`procurement`、`reception`、`reward`、`sales`、`security`、`ticketing`、`welfare`。

### 7.3 所有权规则

- 每个业务对象只能有一个 System of Record 和一个权威主表。
- 组织、员工、客户、供应商、商户、产品、资产和位置必须引用主数据 ID；允许保存业务发生时名称快照，但快照不是主数据。
- 模块只能直接写自己拥有的 Schema。
- 跨域不得直接更新对方表；必须调用领域接口或发布命令/事件。
- 跨域只读 JOIN 仅用于同库、稳定、已评审的查询；复杂报表进入读模型或 `sjg_dw`。
- 不得为了页面方便复制一套员工、组织、客户或订单主档。

---

## 8. 统一闭环模型

### 8.1 标准闭环

```text
发起/触发
→ 身份、权限、数据范围、幂等、完整性和前置条件校验
→ 中心受理、审核、复核或批准
→ 资源锁定与专业子任务分派
→ 员工/执行人提交真实执行结果和证据
→ 独立验收或专业确认
→ 金额、库存、积分、权限、票务、档案等影响回写
→ 通知、回执和后续义务核验
→ 归档包完整性校验
→ 业务关闭
```

异常路径：

```text
退回补充 / 驳回终止 / 撤回 / 取消 / 暂停
/ 部分失败 / 自动重试 / 死信 / 人工接管
/ 冲销 / 反向单 / 补偿任务 / 申诉 / 更正 / 补充归档
```

### 8.2 每个流程必须具备的闭环要素

1. 全局唯一业务 ID 和租户内唯一业务编号；
2. `process_code`、流程版本和表单版本；
3. 发起人、当前身份、组织/岗位快照；
4. 唯一主责人、协作人、验收人、知会人；
5. 当前节点、状态维度、前置条件和允许动作；
6. SLA、工作日历、截止时间、暂停记录和升级策略；
7. 业务输入、明细、附件、执行证据和结果；
8. 审批意见、退回原因、驳回原因、取消原因和更正原因；
9. 领域影响：人员、资源、金额、库存、积分、权限、资格、档案；
10. 异常、重试、补偿、人工接管和对账；
11. 通知送达、回执和申诉入口；
12. 归档包、哈希、版本和不可变审计链。

### 8.3 状态维度必须分离

禁止用一个 `status` 同时表达所有含义。至少区分：

- `workflow_status`：草稿、提交、审核、退回、驳回、完成等流程进度；
- `business_status`：待执行、执行中、部分失败、待验收、已验收、关闭等业务结果；
- `validity_status`：有效、暂停、过期、作废；
- `archive_status`：未就绪、待归档、已归档、补充归档；
- `domain_stage`：当前领域阶段。

具体枚举必须取自流程目录和领域字典，不得由页面临时拼接。

### 8.4 动作语义不得混用

- **退回**：回到允许编辑的前置节点，保留原版本、意见和历史。
- **驳回**：当前审批实例终止；重新办理形成新单或新版本。
- **撤回**：只在下一处理人未实质处理且未产生不可逆外部结果时允许。
- **取消**：业务未继续，但必须保存取消原因和已发生影响的处理结果。
- **作废**：保留原事实，改变有效性，不删除历史。
- **冲销**：新增方向相反的流水并引用原流水。
- **更正**：新增更正记录或新版本，不覆盖已签署、已结算、已核销和已归档原件。
- **技术补偿**：修复技术副作用，不代替业务审批。

### 8.5 关闭门槛

任何流程只有同时满足以下条件才允许关闭或归档：

1. 目标达成，或存在已批准的取消/作废结论；
2. 必办审批完成；
3. 必办执行和验收完成；
4. 异常和补偿已完成，或已转为要素完整、可独立追踪的后续义务；
5. 金额、资源、人员、权限、积分和档案影响已回写；
6. 必要人员已送达或通知；
7. 证据和日志完整；
8. 归档包完整性校验通过。

### 8.6 跨中心黄金路径

`P120`—`P126` 使用 `workflow.wf_orchestration_instance` 编排已存在的子流程：

- 不复制子单业务数据；
- 不在编排主单内重新保存子流程明细；
- 编排只保存依赖关系、里程碑、阻塞、补偿和整体关闭条件；
- 子流程仍由各自领域主表和流程实例负责；
- 子流程失败必须明确是否阻断、降级、补偿或人工接管。

---

## 9. 数据库强制规则

### 9.1 基线

- PostgreSQL 16 是数据库基线。
- 数据字典、DDL、关系、索引和流程落表映射是实现依据。
- 禁止“一流程一表”“一表单一表”“一端一套表”。
- 禁止脱离数据字典随意命名近义字段。
- 任何新增表或字段必须更新数据字典、ER、迁移、测试和源字段映射。

### 9.2 命名

- 数据库、Schema、表、列、索引和约束统一 `lower_snake_case`。
- 禁止拼音缩写、中文物理名、`t_` 无意义前缀和难懂缩写。
- 主键：`id uuid`。
- 外键：`<entity>_id`。
- 业务编号：`business_no` 或明确对象号。
- 时间：`*_at`；业务日期：`*_date`。
- 金额：`*_amount`；比例：`*_rate`。
- 布尔：`is_*` 或 `has_*`。
- 状态：明确状态维度，禁止大量含义不明的 `status2`、`flag`、`type1`。

### 9.3 数据类型

- 主键/外键：`uuid`；公网不得暴露可猜测自增 ID。
- 金额：`numeric(18,2)`；禁止 `float/double`。
- 比例：`numeric(9,6)`，数据库存 0—1，展示层转百分数。
- 时间点：`timestamptz`；展示按 `Asia/Shanghai`。
- 业务日期：`date`；不得与时间戳混用。
- 枚举：受字典或 `CHECK` 约束的英文代码；中文只作展示。
- 动态结构：`jsonb`，必须带 Schema 版本和校验；核心查询字段不得只放 JSON。
- 手机、证件、银行卡等：密文列 + HMAC/哈希检索列；明文不得建索引。

### 9.4 主表、明细和动态字段

- 主表只保存聚合根稳定事实。
- 人员、物料、资源、金额、任务等一对多数据进入明细表，禁止逗号拼接 ID。
- 低频、可配置字段可进入 `workflow.wf_submission_value` 等类型化动态值表。
- 高频查询、排序、统计、权限或唯一性字段必须升级为物理列并执行迁移。
- 动态表单实例必须绑定 `form_schema_version` 和内容哈希。
- 不得以 JSONB/EAV 逃避关系建模、约束和索引设计。

### 9.5 公共字段

正式业务主表原则上具备：

```text
id
 tenant_id
 business_no
 process_instance_id
 status / 分离状态维度
 owner_employee_id
 owner_org_unit_id
 version_no
 created_at
 created_by
 updated_at
 updated_by
 submitted_at
 completed_at
 deleted_at（仅允许逻辑删除的对象）
 correlation_id
```

是否必填、精确列名和字段类型以数据字典为准，不得机械给每张表复制无意义字段。

### 9.6 租户、组织和 RLS

- 所有业务查询首先绑定 `tenant_id`。
- 所有带租户数据的表必须启用 PostgreSQL RLS。
- 应用连接不得拥有 `BYPASSRLS`。
- 业务唯一键必须包含租户维度。
- 组织范围、本人、责任人、授权项目和有效期由 ABAC 继续收窄。
- 禁止遗漏租户条件后依赖前端筛选。
- RLS、服务层权限和字段权限必须分别测试。

### 9.7 事务、幂等和并发

写操作顺序：

1. 认证、当前岗位身份、MFA 和数据范围；
2. 校验 `Idempotency-Key`；
3. 读取并锁定聚合根，或校验 `version_no`；
4. 校验字段、字典、金额、日期、附件、规则和流程版本；
5. 同一事务写领域事实、明细、流程动作和 Outbox；
6. 提交后异步通知、外部同步、审计和分析；
7. 异步失败重试，超限进入死信和人工补偿。

强制规则：

- 每个关键写接口必须有业务幂等键。
- 重复请求返回第一次已成功结果，不重复创建单据和副作用。
- 更新必须使用乐观锁或明确的行锁策略。
- 库存、床位、车辆、场地、票码等资源使用唯一约束、排他约束或锁防止超卖。
- 外部回调以 `provider + external_event_id` 防重。

### 9.8 不可变流水

以下采用不可变流水或版本链：

- 金额和结算；
- 库存出入库；
- 积分；
- 餐卡；
- 票务核销；
- 电子签署；
- 权限授予/回收；
- 关键状态历史；
- 审计。

余额必须能由“期初 + 流水”复算。纠错新增冲销或更正，不改原流水。

### 9.9 附件和文件

- 数据库不保存大文件 Base64/BLOB 作为业务附件。
- 文件写入 S3/MinIO；数据库保存对象键、版本、MIME、大小、SHA256、敏感级别、保留策略和业务关联。
- 上传必须校验扩展名、MIME、大小、病毒、图像解码、恶意压缩包和文件名。
- 下载使用短时签名 URL；敏感下载需要权限、MFA、水印、用途和审计。
- 签署与证据文件启用对象版本化和 WORM/对象锁。

### 9.10 迁移

- 所有结构变更必须通过版本化迁移。
- 生产迁移不得依赖 ORM 自动同步。
- 使用 Expand → Migrate → Contract，禁止同一发布直接删旧列并切换全部代码。
- 迁移必须可重复检测、提供前置检查、数据回填、验证 SQL、回退或前滚方案。
- 大表索引使用低锁方式；生产环境按需 `CONCURRENTLY`。
- 禁止启动时偷偷删表、重建库或清空数据。
- 禁止直接在生产手工改表、改状态、改余额和删审计。

### 9.11 索引、分区、保留和备份

- 组合索引通常以 `tenant_id` 开头。
- 待办常用：`(tenant_id, status, due_at)`。
- 责任人常用：`(tenant_id, owner_employee_id, status)`。
- 低选择性布尔列不单独建索引；可使用部分索引。
- JSONB 只为明确路径建表达式/GIN 索引。
- 预计 2,000 万行以上、50GB 以上或明显按时间归档时才分区。
- 流程日志、审计日志、接口日志、Outbox 可按月分区，并提前创建未来分区。
- `sjg_oms` 目标 RPO ≤ 5 分钟、RTO ≤ 30 分钟；`sjg_audit` RPO ≤ 1 分钟、RTO ≤ 60 分钟；`sjg_dw` RPO ≤ 30 分钟、RTO ≤ 4 小时。
- 备份成功不等于恢复成功；季度必须执行隔离恢复演练。

---

## 10. 权限、隐私和安全

### 10.1 权限模型

必须同时使用：

- RBAC：角色决定基础动作；
- ABAC：租户、组织路径、中心、岗位、本人关系、责任人、项目、状态、风险、时间和有效期决定数据范围；
- PostgreSQL RLS：租户隔离；
- 字段级权限：决定 P2/P3 字段读取、明文查看和导出；
- 职责分离：申请、复核、批准、执行、验收、付款、盖章、发布和审计不得由同一人无监督闭环；
- 临时授权：必须有原因、开始、结束、审批和自动回收；
- 回避：申请人本人、被投诉人、利益冲突人和关联关系人不得审批相关事项。

### 10.2 数据分级统一口径

| 三端字段包 | 平台安全口径 | 含义 |
|---|---|---|
| `P0-公开` | `DATA-L1` | 可公开或低风险展示 |
| `P1-内部` | `DATA-L2` | 企业内部，按岗位和组织访问 |
| `P2-个人` | `DATA-L3` | 个人信息，脱敏、导出审批和访问控制 |
| `P3-高度敏感` | `DATA-L4` | 证件、薪资、健康、签署证据、密钥等，字段加密和逐次审计 |

禁止新增另一套不映射的数据分级。

### 10.3 高风险动作

以下动作原则上需要 MFA、用途说明、四眼复核或按次授权：

- 完整薪资、证件、银行卡、健康数据查看；
- 批量导出和敏感文件下载；
- 权限授予、临时提权和密钥操作；
- 退款、付款、票务人工放行；
- 电子印章、合同作废、签署更正；
- 档案销毁；
- 生产发布、数据库恢复、灾备切换；
- 事故结案和重大责任处理。

### 10.4 服务端强制鉴权

- 隐藏菜单和按钮不是权限控制。
- 每个 API 必须验证用户、当前身份、租户、组织关系、数据范围、动作、字段、状态、有效期、MFA 和利益冲突。
- 搜索、导出、附件、消息链接、批量接口和后台任务必须遵守同一权限。
- 技术管理员默认无业务明文知悉权；故障排查按次授权、限时、脱敏并全程审计。

### 10.5 敏感数据

- 传输全程 TLS。
- 密码使用成熟自适应哈希；禁止自制加密算法。
- P3/DATA-L4 字段使用字段级加密和独立密钥管理。
- 日志、Trace、错误消息、消息队列和分析库不得泄露完整身份证、银行卡、密码、验证码、Token、合同正文和健康资料。
- `localStorage`、URL、前端日志和浏览器缓存不得保存正式 Token 或 P2/P3 业务数据。
- 秘钥、密码、证书只进入密钥管理和环境注入，不写仓库、镜像和 `.env.example` 实值。

### 10.6 Web 安全

必须防护并测试：

- 水平/垂直越权；
- SQL/命令/模板注入；
- XSS、CSRF、SSRF；
- 路径穿越和任意文件访问；
- 恶意上传和压缩炸弹；
- 会话固定、重放和 Token 泄露；
- 开放重定向；
- Webhook 伪造和重放；
- 依赖漏洞；
- 暴力登录、验证码滥用和接口刷取。

---

## 11. 后端代码规则

### 11.1 分层

推荐层次：

```text
Controller / Transport
→ Application Use Case
→ Domain Aggregate / Domain Service
→ Repository Port
→ Persistence / External Adapter
```

强制规则：

- Controller 只负责协议解析、鉴权上下文、DTO 校验和响应映射。
- Controller 禁止直接操作 Repository、SQL 或流程表。
- 领域状态通过聚合方法或明确领域服务改变，不直接赋值。
- 应用服务负责事务边界、幂等、权限策略调用、流程命令和 Outbox。
- Repository 只处理持久化，不包含审批、金额、权限和状态业务判断。
- 外部系统通过 Adapter 隔离，领域层不得依赖供应商 SDK。

### 11.2 Java、TypeScript 与输入校验

- Java统一使用21，生产代码必须通过编译器严格检查和项目静态分析。
- DTO优先使用不可变对象或Java `record`；领域实体不得直接作为API请求/响应对象。
- 服务端输入使用Jakarta Validation和领域级二次校验；Controller校验通过不代表业务规则通过。
- 禁止滥用Lombok隐藏领域行为；若现有项目已使用，只允许用于低风险样板代码，不得用 `@Data` 暴露聚合可变性。
- TypeScript必须开启 `strict: true`。
- 禁止业务代码使用隐式 `any`、无理由 `as any`、`@ts-ignore`、`ts-nocheck`。
- Vue组件Props、Emits、路由参数、Store状态和API返回值必须有明确类型。
- 枚举、错误码、事件和DTO由OpenAPI或统一契约生成/维护，禁止前后端各写一套字符串。
- 金额、比例、日期范围、业务编号、员工ID等关键概念优先使用值对象或受控类型。
- 不信任前端传入的组织、审批人、金额结果、权限、状态和审计字段。

### 11.3 代码组织

- 一个函数只承担一个清晰责任。
- 文件、Vue SFC、函数、`computed`、嵌套深度和圈复杂度必须遵守 16.8 的量化门禁；超过阈值时优先按业务内聚拆分，临时例外必须有 ADR、责任人、到期日和回收任务。
- 禁止巨型 `GodService`、巨型 Controller 和跨全部模块的万能 Repository。
- 禁止复制粘贴 126 套相同 CRUD；复用流程、表单、权限和审计基础能力，同时保留领域规则差异。
- 禁止把复杂业务判断堆在模板、Vue 组件、数据库触发器或定时任务中。
- 数据库触发器只处理不可绕过的审计、时间戳和结构约束，不承载大段可变业务流程。

### 11.4 错误处理

- 所有错误使用稳定错误码和 `correlation_id`。
- 用户提示清晰、可修复，不暴露堆栈、SQL、路径和第三方密钥。
- 技术细节进入结构化日志和 Trace。
- 禁止捕获异常后静默返回成功。
- 禁止用 `null`、空字符串或 HTTP 200 表示业务失败。
- 外部依赖失败要区分可重试、不可重试、部分失败和人工接管。

### 11.5 定时任务和批量任务

- 必须有唯一任务键、租户范围、批次、断点、重试、最大次数、死信和运行审计。
- 批量任务分批提交，避免长事务和全表锁。
- 定时任务必须可重入、幂等，支持手工重跑指定批次。
- 任务状态不得只存在内存或 Redis。


### 11.6 Spring JDBC 持久化强制规则

- Repository通过 `JdbcTemplate` 或 `NamedParameterJdbcTemplate` 访问数据库；Controller和领域对象禁止执行SQL。
- 多参数、可选筛选和批量查询优先使用命名参数，禁止字符串拼接参数和值。
- 所有SQL必须参数化；动态排序、表名、列名和筛选字段必须使用白名单映射。
- 禁止生产查询使用无边界 `SELECT *`；明确列名、稳定别名和RowMapper映射。
- 每个聚合根有明确Repository；禁止创建跨265张表的万能DAO或通用CRUD仓库。
- `@Transactional`原则上放在Application Service公共方法；禁止在Controller和Repository随意扩大事务边界。
- 金额、库存、积分、票务、权限和状态写入必须在同一事务中写业务事实、状态历史、审计要求和Outbox。
- 批量写使用 `batchUpdate` 或受控COPY方案，按批次提交并记录失败行；禁止逐行N+1写入。
- 并发更新必须使用 `version_no`、条件更新、唯一约束或显式锁，更新影响行数不符合预期必须报冲突。
- PostgreSQL特性如CTE、窗口函数、JSONB、部分索引、排除约束、RLS和分区可以使用，但必须有测试和数据字典说明。
- RLS会话上下文必须在连接/事务范围安全设置并清理，不得因连接池复用导致租户或数据范围串线。
- Repository集成测试必须使用PostgreSQL 16 Testcontainers或等价真实PostgreSQL环境，不使用H2模拟。
- 禁止引入JPA实体、Hibernate Session、自动脏检查和Schema自动同步。

---

## 12. API 和事件规范

### 12.1 API

- 使用明确版本：`/api/v1/...`。
- 资源名使用复数、英文、稳定语义。
- 列表必须分页，默认限制和最大限制固定；禁止无界返回。
- 筛选和排序字段必须白名单化。
- 关键写接口要求 `Idempotency-Key`。
- 并发更新使用 `version_no`、`If-Match` 或等价机制。
- 业务动作优先使用明确动作端点，例如：

```text
POST /api/v1/expense-claims/{id}/actions/submit
POST /api/v1/expense-claims/{id}/actions/approve
POST /api/v1/expense-claims/{id}/actions/return
POST /api/v1/expense-claims/{id}/actions/cancel
```

禁止通用 `PATCH status=<任意值>`。

### 12.2 响应和错误

- 成功响应只返回当前权限允许的字段。
- 分页响应至少包含 `items`、游标/页信息和是否还有更多。
- 错误使用统一 Problem Details 或项目稳定结构：错误码、用户消息、字段错误、`correlation_id`。
- 生产响应不得返回堆栈、SQL、内部路径和第三方原始报文。

### 12.3 OpenAPI 和契约

- OpenAPI 必须由真实代码或契约生成并进入 CI 校验。
- API 变更必须检查向后兼容性。
- 前端 API Client 应由 OpenAPI/契约生成或集中维护，禁止页面手写不同口径。
- 数据库模型不直接暴露为 API DTO。

### 12.4 领域事件

事件至少包含：

```text
event_id
 event_type
 event_version
 tenant_id
 aggregate_type
 aggregate_id
 occurred_at
 actor_id
 actor_identity_id
 correlation_id
 causation_id
 payload
```

规则：

- 事件名称使用稳定英文语义和版本。
- 事件在业务事务内写入 Outbox。
- 消费者使用 Inbox/事件唯一键保证幂等。
- 事件 Payload 不携带非必要 P2/P3 明文。
- 事件 Schema 变更必须向后兼容或发布新版本。

### 12.5 Webhook

- 验证签名、时间戳、Nonce 和重放窗口。
- 先落接收记录，再异步处理。
- 以供应商和外部事件 ID 防重。
- 重复回调返回相同处理结果。
- 不得在 Webhook 请求线程内完成长事务和多级外部调用。

---

## 13. 表单和字段实现规则

### 13.1 字段来源

每个流程三端 Excel 固定包含：流程总览、表单清单、字段字典、状态与审批、规则与接口、三端联动。实现前必须读取对应流程的三端资料，不得只看员工端页面。

### 13.2 字段元数据

表单字段至少具备：

- 稳定字段编码；
- 中文名称；
- 数据类型；
- 必填条件；
- 默认值规则；
- 枚举/字典来源；
- 长度、范围、精度和跨字段校验；
- 数据来源与是否可编辑；
- 敏感级别；
- 三端可见、可编辑和可导出权限；
- 生效版本；
- 目标表/字段或动态值承载路径；
- 审计和保留策略。

### 13.3 草稿、提交和字段级退回

- 草稿允许流程后续节点才必填的字段为空。
- 提交时按当前节点和表单版本执行服务端条件必填。
- 字段级退回只解锁被退回字段及其受影响字段；已通过字段保持锁定和版本。
- 退回保留原值、修改值、原因、审核人和时间。
- 自动保存必须有版本和冲突处理，不能静默覆盖另一设备修改。

### 13.4 重复明细

- 多人、多物料、多票据、多资源、多金额等使用数组 DTO + 明细表。
- 每个明细有独立 ID、顺序、状态、校验和审计。
- 删除已产生影响的明细必须走取消、冲销或作废，不能物理删除。

### 13.5 动态配置

- 正式表单 Schema 只能经过草稿、审核、发布、版本化流程。
- 已启动实例绑定启动时版本，默认继续旧版。
- 迁移进行中实例到新版本必须有映射、校验、影响说明和审计。
- 规则表达式使用受控 DSL/表达式引擎，禁止 `eval` 和管理员任意脚本。

---

### 13.6 前端表单校验与边界规则

- HTML 的 `required`、`min`、`max` 和输入类型只属于第一层体验校验，不能作为完整业务校验。
- 前端必须使用项目统一的 Schema 校验方案。若仓库已有 Zod、Valibot 或等价方案，必须继承；若尚未选择，只能通过 ADR 统一选择一种，禁止同一仓库并行引入多套 Schema 库。
- 前端 Schema 至少覆盖：去空格、空值、长度、数值范围、精度、UUID/业务编号格式、枚举、日期先后、跨字段依赖、数组明细、文件类型与大小。
- `createDelegation`、`registerFile`、`startWorkflow`、审批动作等关键写操作，必须在调用 API 前完成结构化校验，并在服务端再次执行 Jakarta Validation 与领域规则校验。
- 日期计算必须显式处理 `null`、无效日期、时区和边界；禁止用固定“年毫秒数”近似自然年、工龄、合同期和 SLA。
- 搜索词、排序字段、筛选字段和正则输入必须受控；用户输入不得直接进入正则、SQL 片段、路径、列名或排序表达式。
- 表单错误必须映射到具体字段和可修复提示；跨字段或服务端业务错误进入表单级错误区，不得只弹出通用“操作失败”。
- 服务端是最终裁决者。前端校验通过不得被当作权限、状态、金额、审批人或业务规则已经通过。

---

## 14. 流程、规则、任务和 SLA

### 14.1 流程版本

- 流程定义、节点、动作、表单、规则、审批人解析、SLA 和通知模板必须版本化。
- 只有已发布版本可启动生产实例。
- 发布后不可原地修改；修改形成新版本。
- 运行中实例固定版本，除非通过受控迁移。

### 14.2 状态迁移计算

服务端按以下顺序计算：

```text
当前实例与版本
+ 当前节点
+ 请求动作
+ 当前岗位身份
+ RBAC/ABAC/字段权限
+ 回避与职责分离
+ 业务前置条件
+ 规则结果
+ 外部不可逆事实
= 下一节点、状态、任务和副作用
```

前端提交的目标状态仅可忽略或拒绝，不得信任。

### 14.3 审批人和任务

- 审批人按流程版本、组织、岗位、项目、金额、风险和替岗规则动态解析。
- 无合法审批人时升级，不得默认通过。
- 任务必须记录候选人、实际处理人、接受时间、完成时间、转交和代理关系。
- 本人审批本人、被投诉人审批案件、执行人独立验收本人结果必须被阻断。

### 14.4 SLA

- SLA 从任务实际接收或定义的起点开始。
- 使用版本化工作日历。
- 暂停使用独立暂停记录，不直接改写原截止时间。
- 超时路径至少支持提醒、催办、升级和高风险人工接管。
- SLA 参数不得硬编码在页面或 Controller。

### 14.5 技术后台修复边界

技术后台可以：

- 重新投递事件；
- 重试接口；
- 处理死信；
- 执行已批准补偿命令；
- 恢复卡死任务；
- 修复投影和索引；
- 发起数据质量工单。

技术后台禁止：

- 任意设置业务状态；
- 直接改金额、库存、积分和票务余额；
- 删除失败记录让指标“变好看”；
- 绕过审批调用印章、付款、退款和权限；
- 通过改库伪造已执行、已验收、已送达或已归档。

---

## 15. 外部集成和异步可靠性

### 15.1 适配器

- 身份、短信、邮件、企业微信/钉钉、签章、支付、票务、考勤机、门禁、定位、财务和 OTA 均通过适配器。
- 领域代码只依赖内部端口和稳定模型。
- 供应商字段、错误码和 SDK 不得渗透到领域实体。

### 15.2 超时、重试和熔断

- 每个外部调用必须设置连接和请求超时。
- 只对明确可重试错误使用指数退避和抖动。
- 写操作重试必须有幂等键。
- 达到重试上限进入死信和人工补偿，禁止无限重试。
- 外部系统故障不得拖垮登录、工作台和无关模块。

### 15.3 对账和补偿

- 支付、退款、票务、签章、库存和权限必须有状态查询或日终对账能力。
- 本地“请求已发送”不能等于外部“已成功”。
- 部分失败要明确已成功子项，禁止重复执行。
- 补偿命令必须引用原业务、原事件和原副作用。

### 15.4 环境隔离

- 开发、测试、预生产、生产的数据库、对象存储、消息、短信、签章、支付和门禁完全隔离。
- 测试环境默认使用模拟/沙箱适配器并带明显测试标识。
- 禁止开发环境误调用生产印章、支付、短信和正式权限。

---

## 16. 前端与交互规则

### 16.1 三端体验差异

- 员工端：移动优先、单任务优先、语言温暖、下一步明确。
- 中心管理端：桌面优先、待办与风险优先、支持批量但必须预览和审计。
- 技术后台端：运行健康、故障、配置、补偿、审计优先；不默认显示业务敏感明细。

### 16.2 统一设计系统

- 必须遵循最新根 `DESIGN.md`（V2.0 起为员工端、中心端、技术端统一设计与 Vue 实现基线）；禁止另起一套颜色、间距、字体和组件体系。
- 旧 DESIGN、旧 HTML/React 原型、Tailwind 示例只能作为视觉参考；与根 `DESIGN.md`、根 `AGENT.md` 或实际 Vue 工程冲突时，必须按当前 canonical 基线修正，不得引入第二技术栈。
- 三端共享无业务语义的 UI 组件、状态标签、表单控件、权限守卫和可访问性能力。
- 业务模块可以扩展，但必须通过设计令牌。
- 中文正文桌面端原则上不小于 16px，移动端不小于 15px；关键说明不得做成难读小灰字。
- 触控目标不小于 44×44px。
- 颜色不能成为唯一状态表达。

### 16.3 状态与真实反馈

每个页面必须具备：

- 加载；
- 空状态；
- 无权限；
- 校验失败；
- 冲突；
- 外部依赖失败；
- 部分失败；
- 重试/人工帮助；
- 成功后的真实服务端结果。

禁止：

- 点击后先显示成功，再后台“尽力保存”；
- 用本地状态伪造流程完成；
- 只做静态页面、假按钮、假图表或不可持久化演示数据却声称系统完成；
- 生产页面保留测试手机号、验证码、默认密码、邀请编号和临时通道；
- 仅通过隐藏按钮实现权限；
- 将身份证、合同、银行卡、正式成绩和 Token 放入 `localStorage`。

### 16.4 表格和列表

- 默认分页、筛选、排序和明确数据范围。
- 大数据列表使用服务端查询和虚拟化；禁止一次加载全部。
- 行操作必须根据服务端允许动作返回，不只靠前端猜测状态。
- 移动端使用卡片、分组或受控横向滚动，不压缩到不可读。

### 16.5 员工文案

员工端文案必须：

- 准确、尊重、清楚；
- 说明为什么、下一步和如何修复；
- 不把后台风控、责任认定和技术错误原样甩给员工；
- 不用恐吓、羞辱和空洞口号；
- 提供人工帮助入口。

### 16.6 入职模块已确认限制

在新人入职相关页面继续遵守：

- 首页只保留“我是老员工”“我是新员工”主入口；
- 保留“今天不用记住所有事情，我们会一步一步陪你完成。”；
- 不显示企业邀请编号、测试账号、测试验证码和独立测试说明；
- 不提供“减少动态效果”按钮；可以静默尊重系统辅助偏好；
- 不提供“打印入职包”功能；正式合同等受控文书打印除外；
- 未达到结业与权限门禁时，主工作台路由、菜单、API 和数据均不可达；
- 不因结业自动开放所有高风险权限。


### 16.7 Vue 3 工程规则

- 统一使用Vue 3 Composition API和 `<script setup lang="ts">`；禁止新写Options API，除非维护现有遗留组件。
- 页面组件负责展示与交互编排，领域规则、权限结论和最终状态不得放在组件中计算。
- Pinia Store按领域或业务能力拆分；禁止单一全局巨型Store。
- 服务端业务数据以查询结果为准；Pinia不得长期缓存完整敏感档案、薪资、合同正文、身份证和银行卡。
- 路由守卫只做前端体验门禁；每个API仍须后端鉴权、数据范围和字段权限校验。
- API客户端由OpenAPI生成或统一封装，禁止页面各自拼接URL和重复定义响应类型。
- 使用 `vue-tsc --noEmit` 进行SFC类型检查，不能只依赖Vite构建成功。
- 单元测试使用Vitest + Vue Test Utils，关键闭环使用Playwright覆盖PC与移动视口。
- 三端共享设计令牌和无业务语义组件，但员工端、中心端、技术端的页面结构和交互目标必须分别设计。
- 组件卸载、路由切换和请求取消必须正确处理，禁止陈旧响应覆盖新状态。
- 环境变量只允许暴露以批准前缀声明的公开配置；密钥、数据库地址和服务凭据不得进入前端构建产物。

---

### 16.8 Vue 单文件组件与函数复杂度门禁

以下为新建或修改代码的默认强制阈值；空行、纯注释和自动生成代码不计入有效行数：

| 项目 | 默认上限 | 超限处理 |
|---|---:|---|
| 页面容器 SFC 有效行数 | 300 行 | 拆为页面壳、特性组件、Composable、Selector/Mapper |
| `<script setup>` 有效行数 | 180 行 | 迁出请求、状态、选择器和业务动作 |
| `<template>` 有效行数 | 180 行 | 提取可复用展示组件和领域特性组件 |
| 单个函数有效行数 | 40 行 | 拆分校验、转换、请求、状态更新和副作用 |
| 控制流/回调嵌套深度 | 3 层 | 使用提前返回、纯函数、映射表或策略对象 |
| 单文件顶层函数数量 | 15 个 | 按特性拆分 Composable/Service |
| 单个 `computed` 有效行数 | 25 行 | 提取纯 Selector，并单独测试 |
| 圈复杂度 | 10 | 分解分支或使用明确状态/策略模型 |

- 阈值不是鼓励写到上限。组件应围绕单一页面责任或单一业务能力组织。
- `computed` 必须无副作用，不发请求、不改 Store、不写浏览器存储；连续多层 `filter/map/reduce/find` 应提取为命名 Selector。
- 复杂页面必须采用“页面壳 + 特性区块 + Composable + API Service + 纯 Selector/Mapper”结构，禁止把 50 个函数和 20 个 `computed` 堆在一个 SFC。
- 自动生成文件必须放入明确的 `generated` 目录并禁止手改，才可豁免行数门禁。
- 遗留超限文件执行质量棘轮：任何修改不得增加有效行数、复杂度和顶层函数数；触及核心逻辑时至少抽取一个可独立测试单元。确需临时例外必须有 ADR、责任人、到期日和回收任务。

### 16.9 三端共享与重复代码治理

- `employee-web`、`center-web`、`tech-web` 只能保留端侧路由、页面组合、布局和端侧策略；共享契约、会话、请求状态、通用业务特性和无业务语义 UI 必须进入共享包。
- 推荐补充以下共享层：

```text
frontend/packages/
  ui/                    # Avatar、StatusChip、RecordCard、PersonRow 等无业务语义组件
  contracts/             # OpenAPI 类型、权限码、错误码、稳定枚举
  session/               # 会话、身份切换、Step-Up、权限展示辅助
  api-client/            # 统一请求、错误映射、重试、取消、幂等头
  validation/            # 统一 Schema、字段错误映射
  features/<domain>/     # 可跨端复用的领域特性与 Composable
```

- 禁止把同一个 `CenterPortal.vue`、`EmployeePortal.vue`、组织图、审批中心或同类文件复制到三个应用后分别维护。
- 两个应用间出现 **80% 以上相似文件**，或出现 **50 行以上相同有效代码片段**，默认视为应抽取的重复实现；确属端侧差异时必须在代码审查中说明差异理由。
- 全仓库复制率目标必须低于 15%；本次新增或修改代码复制率不得高于 5%；任何精确复制的跨端门户文件均直接阻断合并。
- 共享包通过 Props、Slots、策略接口、权限能力和端侧适配器表达差异，禁止用大量 `if (appType === ...)` 构造新的巨型共享组件。
- 共享不应把业务语义错误合并。只有状态、校验、权限语义和数据契约相同的能力才可共用；不同流程必须保留各自 `process_code` 和领域规则。

### 16.10 异步请求、错误边界和竞态处理

- 所有前端 API 调用必须经过统一 `api-client` 和类型化 Service，页面不得自行拼接 URL、Token、Step-Up、重试和错误映射。
- 资源加载与动作提交必须采用统一的 `AsyncState<T>`/Composable，至少区分：`idle`、`loading`、`success`、`empty`、`partial`、`error`、`cancelled`。
- 禁止用一个全局 `busy` 布尔值管理多个并行请求；每个资源或动作拥有独立状态，页面再计算聚合状态。
- 每个 `async` 动作必须完成必要清理。`catch {}`、空 `catch`、仅调用 `session.showError(value)` 后继续当作成功，均视为失败实现。
- 捕获异常后必须选择其一：映射为稳定类型化错误并返回失败结果；记录必要上下文后重新抛出；进入明确补偿/重试路径。不得静默吞掉。
- 错误上报至少包含操作名、`process_code`、请求/关联 ID 和错误分类；不得包含 Token、证件、薪资、合同正文等敏感数据。
- 路由切换、搜索词变化、业务 ID 变化和组件卸载必须使用 `AbortController`、请求序号或等价“最后一次请求生效”机制，防止旧响应覆盖新状态。
- `loadAll()` 不得继续累积大量条件分支。独立资源可使用具名任务表与 `Promise.allSettled` 返回部分失败；关键依赖必须明确失败边界，不能用一个通用成功提示掩盖缺失数据。
- 自动重试只适用于明确可重试且幂等的操作。非幂等写请求没有 `Idempotency-Key` 时禁止自动重试。
- Vue 应用必须配置全局错误处理、路由错误处理和组件级错误边界；错误边界只负责兜底展示与报告，不能替代局部业务错误处理。

### 16.11 常量、权限码、日期和标识符

- 权限码、数据分级、业务枚举、动作码、错误码和事件名必须来自 `contracts` 或服务端契约；禁止在页面散落 `'GENERAL'`、`'DATA_L2'`、`'HR_CONFIGURATION'` 等裸字符串。
- 纯 UI 常量、分页上限、显示数量和格式规则必须使用有语义的命名常量；`.slice(0, 5)` 等魔法数字必须说明业务含义。
- 日期、工龄、合同期和 SLA 使用日期时间 API 与业务日历计算；禁止 `31_556_952_000` 等固定毫秒常量替代自然年。
- 权限展示统一通过 `session.can(...)` 或批准的能力接口；页面中的权限判断只改善体验，后端仍必须二次校验。
- 数据库主键、正式业务编号和流程实例 ID 原则上由服务端生成。前端 `crypto.randomUUID()` 只可用于 `Idempotency-Key`、`correlation_id`、临时草稿键或协议明确允许的客户端 ID。
- `center-workflow-*`、`center-request-*`、`center-file-*` 等字符串前缀不得作为正式业务主键、权限依据或审计事实。确需可读业务编号时由服务端统一序列规则生成。
- 常量文件按领域和用途拆分；禁止建立一个全仓库万能 `constants.ts` 杂物箱。

---

## 17. 审计、日志和可观测性

### 17.1 审计内容

关键操作至少记录：

- 租户；
- 操作者和当前岗位身份；
- 动作和对象；
- 业务编号、流程实例和节点；
- 前后值摘要；
- 原因和审批意见；
- 流程、规则、表单和字典版本；
- 请求 ID、Trace ID、设备、IP；
- 时间；
- 附件哈希；
- 结果和错误码。

审计库应用角色无 `UPDATE/DELETE/TRUNCATE` 权限。

### 17.2 日志

- 使用结构化日志。
- 每个请求有 `correlation_id` 和 Trace ID。
- 日志分级准确；可预期业务拒绝不应大量记为系统 ERROR。
- 日志脱敏，禁止完整 P2/P3 数据。
- 不记录密码、验证码、Token、私钥、完整合同正文和文件内容。

### 17.3 指标和 Trace

至少监控：

- 请求量、P50/P95/P99、错误率；
- 数据库连接、慢 SQL、锁、WAL、复制延迟和磁盘；
- Redis 命中和容量；
- Outbox 积压、消费延迟、重试和死信；
- 外部接口成功率、延迟和错误分类；
- 定时任务成功率；
- 各流程状态分布、超时和异常数量；
- 对象存储失败和签名 URL 异常。

技术监控可下钻到服务、接口、事件和 Trace，但不得因此绕过业务字段权限。

---

## 18. 非功能要求

### 18.1 性能

正常企业内网和目标并发下：

- 95% 普通查询接口 ≤ 1 秒；
- 95% 普通写接口 ≤ 2 秒；
- 复杂统计 ≤ 5 秒，否则转异步；
- 1,000 行以内标准导出 ≤ 30 秒，超量异步；
- 首页指标使用缓存、读模型或预聚合，禁止每次全表扫描；
- 列表默认分页；
- 禁止 N+1 查询；
- 大文件使用分片或后台任务。

性能必须通过接近目标规模的数据压测，不得只用十几条演示数据。

### 18.2 可用性

- 核心工作时段月度可用性目标 ≥ 99.9%。
- 单个模块或外部系统失败不得导致整站不可用。
- 核心写操作重复点击不得重复执行。
- 发布必须可回退，数据库迁移必须验证。
- 关键页面提供清晰降级、待同步或人工接管状态。

### 18.3 可维护性

- 领域边界清晰；
- 规则集中并有单元测试；
- 数据库迁移版本化；
- 配置、流程和模板可追踪；
- OpenAPI 自动生成；
- 关键复杂业务代码有中文业务注释和设计说明；
- 技术债必须进入任务系统，有责任人和回收计划。

### 18.4 可访问性与兼容性

- 支持主流 Chrome、Edge 和企业移动 WebView。
- 支持键盘、清晰焦点、语义标签和屏幕阅读器基础能力。
- 图片有替代文本，视频有字幕。
- 错误不只依赖颜色。
- 支持系统字体放大。
- 动画不闪烁、不阻塞主任务。
- 页面在常见手机尺寸无需整体横向缩放。

### 18.5 弱网

可暂存：普通草稿、静态内容、受控课程缓存、离线执行草稿。  
不可离线伪完成：正式合同签署、印章、付款、退款、最终成绩、高风险权限、重大审批和敏感导出。

---

## 19. 测试策略和质量门禁

### 19.1 测试层级

1. 单元测试；
2. 领域规则测试；
3. 数据库约束和迁移测试；
4. API 契约测试；
5. 集成测试；
6. 三端 E2E；
7. 权限和安全测试；
8. 异常、补偿和恢复测试；
9. 性能和稳定性测试；
10. 安装、备份恢复和用户验收测试。

### 19.2 每个已实现流程的最小测试矩阵

每个 `process_code` 至少覆盖：

- 员工端发起/执行；
- 中心端审核/分派/验收；
- 技术端配置/监控/补偿；
- 正常闭环；
- 退回补充；
- 驳回终止；
- 撤回或取消的合法/非法窗口；
- 重复提交和重复回调；
- 越权和本人审批本人；
- 过期版本和并发冲突；
- 外部依赖超时、重试、死信和人工接管；
- 附件安全；
- P2/P3 字段可见和导出；
- 审计完整；
- 归档门槛；
- 刷新、跨端和跨设备状态一致。

### 19.3 数据库测试

必须验证：

- 迁移从空库成功；
- 从上一版本升级成功；
- 前置检查和回填正确；
- 外键、唯一约束、Check、排他约束和索引有效；
- RLS 不跨租户；
- 不可变流水不可覆盖；
- 幂等键防重复；
- 乐观锁拒绝旧版本；
- 归档后不可直接编辑；
- 备份可恢复并通过业务校验。

### 19.4 全局一致性测试

- 各类正式管理评分使用独立 `score_type`，生产满分统一为 1000；不同分数不得相加、平均或替代。
- 岗位等级、绩效等级、奖励、纪律、成长段位、荣誉积分和成长积分命名空间隔离。
- 同一事实不得重复建案、重复扣绩效、重复计积分或重复提成。
- `workflow_status`、`business_status`、`validity_status`、`archive_status`、`domain_stage` 不互相覆盖。
- 作废事实可以归档，但不可物理删除。
- 自然人和员工关系分离；重新入职创建新员工关系，历史不回退。
- 合同关系、合同版本和签署档案分离。

### 19.5 覆盖率和门禁

- 核心领域代码行覆盖率目标 ≥ 80%。
- 关键规则分支覆盖率目标 ≥ 90%。
- `DEFECT-S0` 为 0。
- `DEFECT-S1` 为 0；确需例外必须由业务、技术和安全书面接受。
- Lint、格式化、类型检查、单元、集成、迁移、契约和关键 E2E 必须全部通过。
- 不得通过删除测试、降低断言、跳过安全校验或扩大 `any` 来“修复”CI。

### 19.6 禁止伪造测试

- 禁止只断言 HTTP 200 而不验证领域事实、流水、事件和审计。
- 禁止使用与生产数据库语义不一致的内存数据库替代集成测试。
- 禁止对失败测试无解释地 `skip`。
- 禁止把随机等待当作异步稳定性方案。
- 测试数据必须使用虚构信息，不得复制真实员工敏感数据。

---

### 19.7 前端静态质量门禁

CI 必须对前端执行以下自动检查，工具可以使用等价实现，但规则不得缺失：

1. `vue-tsc --noEmit`：SFC 严格类型检查；
2. ESLint：禁止空异常、未处理 Promise、生产 `console`、隐式/显式滥用 `any`、无效禁用注释；
3. 复杂度检查：函数行数、圈复杂度、嵌套深度、文件行数和顶层函数数量；
4. 重复代码检测：使用 `jscpd` 或等价工具，检查全仓库和本次变更；
5. 死代码检测：使用 `knip` 或等价工具检查未使用导出、依赖、文件和脚本；
6. 循环依赖检查：共享包、应用和领域特性不得形成反向依赖；
7. Vitest：单元和组件测试；
8. Playwright：关键三端闭环、权限、重复点击、刷新和竞态；
9. 构建产物检查：不得包含密钥、测试账号、源码映射敏感内容和未批准环境变量。

默认 ESLint/质量阈值与 16.8 一致：`max-depth=3`、函数有效行数不超过 40、圈复杂度不超过 10。配置应排除生成目录，但不得大范围忽略 `src`。

质量棘轮规则：

- 存量指标尚未达标时，CI 先固定基线；每次变更不得让重复率、复杂度、类型错误、死代码和测试覆盖率恶化。
- 新文件和重构后文件必须立即满足新阈值，不能以“遗留项目”为理由新增同类问题。
- 禁止通过全局关闭规则、扩大忽略目录、添加大范围 `eslint-disable`、删除测试或降低阈值让 CI 通过。
- 单点例外必须靠近代码、写明原因、关联任务和到期日；到期后 CI 应重新阻断。
- CI 必须保存质量报告作为构建产物，PR 描述必须列出基线与变更后数字。

### 19.8 前端核心回归测试基线

以下能力在修改前必须先建立特征测试，在修改后必须有回归测试：

- `portal-session.ts`：登录、恢复、损坏缓存、过期会话、岗位切换、权限刷新、退出和并发恢复；
- `api/client.ts`：认证头、Step-Up、401/403、错误映射、超时、取消、可重试错误、非幂等禁止自动重试和 `Idempotency-Key`；
- 门户页面：并行加载、部分失败、旧响应竞态、权限隐藏与 API 拒绝、表单边界、重复点击和刷新一致性；
- 共享组件：`RecordCard`、`StatusChip`、`PersonRow`、头像、空状态、错误状态和可访问性；
- 每个已确认缺陷都必须先有能复现问题的测试，再修复；禁止只改代码不留下回归保护。

覆盖率不能只看总数字。会话、权限、请求重试、幂等、状态迁移和敏感字段过滤属于关键规则分支，必须达到本文件 19.5 的分支覆盖率目标。

---

## 20. Windows、本地开发和部署

### 20.1 环境要求

项目必须支持：

- Windows 10/11；
- 中文目录；
- 路径包含空格；
- Docker Desktop/WSL2 或明确的本地依赖方案；
- 中国网络环境的依赖镜像配置；
- 重复安装和重复启动不破坏数据。

### 20.2 必备脚本

至少提供：

- `一键安装.bat`；
- `一键启动.bat`；
- `一键停止.bat`；
- `一键自检.bat`；
- `一键备份.bat`；
- `一键恢复演练.bat` 或等价受控脚本。

BAT 规则：

```bat
chcp 65001
set "VAR=value"
cd /d "%~dp0"
```

- 所有路径加引号；
- 不假定 C 盘和固定用户目录；
- 不把 PowerShell 语法混入 CMD；
- 失败时输出原因和日志位置，不闪退；
- 不每次重复下载全部依赖；
- 生产恢复、迁移和清理操作必须二次确认并默认拒绝危险默认值。

### 20.3 CI/CD

CI 至少执行：

1. 锁文件一致性；
2. 格式化检查；
3. Lint；
4. 类型检查；
5. 单元测试；
6. PostgreSQL 集成测试；
7. 迁移空库/升级校验；
8. OpenAPI/事件契约兼容性；
9. 依赖和镜像安全扫描；
10. 关键 E2E；
11. 构建和产物清单。

生产发布必须包含：版本、变更说明、迁移、回退、配置变化、监控、负责人和验收证据。


### 20.4 Docker 与生产运行时

- 后端使用多阶段构建：构建阶段使用Java 21 JDK，运行阶段使用受维护的Java 21 JRE镜像。
- 生产容器以非root用户运行，文件系统尽量只读，临时目录和上传目录显式挂载。
- 镜像必须固定可追溯版本或摘要，禁止长期使用漂移的 `latest`。
- 前端在Node构建镜像中完成依赖安装、类型检查和构建；生产镜像只包含静态产物和必要服务器配置。
- 数据库迁移由受控发布步骤或唯一迁移任务执行；禁止每个后端副本并发无序执行Flyway。
- 容器健康检查区分存活、就绪和外部依赖；数据库短暂不可用不得让所有副本进入重启风暴。
- 配置和密钥通过环境、Secret或批准的密钥系统注入，不写入镜像层和Git。
- Docker Compose仅作为本地/测试编排基线；生产部署方式按现有基础设施执行。

---

## 21. AI 智能体标准执行流程

### 21.1 开工前必须完成

1. 读取本 `AGENT.md`。
2. 检查 Git 状态，保护用户未提交修改，不覆盖无关文件。
3. 识别任务对应的 `process_code`、业务域、三端和权威主表。
4. 读取该流程三端 Excel、状态、规则、接口和三端联动资料。
5. 读取相关数据表、字段、关系、索引、DDL 和现有迁移。
6. 读取现有 API、页面、权限、测试和设计规范。
7. 列出影响矩阵：UI、API、领域、数据库、流程、事件、权限、审计、集成、测试和文档。
8. 检查是否存在冲突、重复实现、不可逆动作和数据迁移风险；
9. 运行或读取最新重复率、复杂度、死代码、类型和测试覆盖率基线，确认本次变更不会恶化。

### 21.2 实施顺序

1. 明确客观完成条件和异常路径；
2. 先补测试或写可失败的验收测试；
3. 设计领域命令、状态迁移和权限；
4. 必要时先写向前兼容迁移；
5. 完成后端真实事务闭环；
6. 完成三端需要的 API 和字段过滤；
7. 完成前端交互和真实状态反馈；
8. 完成事件、异步、审计、监控和补偿；
9. 运行完整质量门禁；
10. 更新文档、数据字典、OpenAPI、测试矩阵和 CHANGELOG。

### 21.3 最小垂直闭环

优先完成一条可运行的垂直链路，不先批量生成大量静态页面。一个垂直闭环至少包含：

```text
页面/表单
→ API 契约
→ 服务端校验和权限
→ 领域命令与状态机
→ PostgreSQL 持久化
→ 流程任务和动作日志
→ Outbox/通知/审计
→ 查询和三端回显
→ 正常、退回、越权、重复和失败测试
```

### 21.4 遇到歧义

按顺序：

1. 搜索仓库、本文件和项目资料；
2. 对照 `process_code` 和数据库落表映射；
3. 选择合法、最小权限、可逆、风险最低方案；
4. 在 `docs/assumptions.md` 记录；
5. 仅当不可逆高风险问题仍无法判断时，提出一个精准问题；
6. 不得用大量泛泛问题阻塞可安全完成的工作。

### 21.5 失败和部分完成

- 不得声称完成而没有代码、迁移或测试证据。
- 工具、环境或依赖失败时，保留可运行状态，说明已完成、未完成、失败原因和下一步命令。
- 不得用假按钮、TODO、空实现、硬编码假数据掩盖失败。
- 不得擅自回滚或删除用户已有修改。

### 21.6 每次交付报告格式

每次任务完成后必须报告：

1. 对应流程编码；
2. 完成的业务闭环；
3. 修改文件；
4. 数据库迁移及回退策略；
5. 权限和敏感数据影响；
6. 事件、接口和补偿影响；
7. 实际运行的测试和结果；
8. 尚存限制、风险和未执行验证；
9. 启动、验证和回滚命令。

---

### 21.7 遗留门户渐进式重构流程

当任务涉及巨型门户组件、三端复制或重复请求逻辑时，智能体必须按以下顺序执行，不得直接整文件推倒重写：

1. **建立基线**：运行现有构建、类型检查、测试和关键页面；记录当前路由、API、权限、截图和已知失败。
2. **补特征测试**：先覆盖当前正确行为、已知缺陷和关键权限，避免重构时无意改变业务语义。
3. **建立共享边界**：先创建/完善 `ui`、`contracts`、`session`、`api-client`、`validation` 和领域 `features`，不得先复制第四份代码作为“过渡”。
4. **先抽纯展示**：从重复模板中提取 `RecordCard`、`StatusChip`、`PersonRow` 等无业务语义组件。
5. **再抽纯逻辑**：把筛选、排序、日期、标签和映射提取为无副作用 Selector/Mapper，并补单元测试。
6. **再抽请求与动作**：把 `loadXXX`、错误处理、取消、并发和动作提交迁入类型化 Service/Composable。
7. **拆分特性区块**：将巨型门户拆为 8—10 个左右可独立测试的功能区块只是参考目标，必须按真实业务内聚划分，不能机械按行数切文件。
8. **保留端侧差异**：三端页面壳分别负责布局、入口、数据范围和动作组合；共享特性不得绕过端侧权限策略。
9. **逐步替换**：每一步都必须可编译、可运行、可回退；新实现稳定并通过测试后再删除旧代码。
10. **小提交交付**：依次提交共享基础、展示组件、逻辑抽取、请求抽取、页面替换和清理；禁止把依赖升级、大规模格式化和业务重构混成一个提交。

重构过程中必须保持 API 契约、路由地址、业务编号、权限语义和持久化事实不变。任何行为变化必须单独列为业务变更，按流程、迁移和验收处理，不得隐藏在“代码整理”中。

---

## 22. 变更影响矩阵模板

开始重大修改前，在 PR、任务或设计文档中填写：

| 项目 | 内容 |
|---|---|
| 流程编码 | `Pxxx` |
| 业务目标 | 可验证的目标，不写“优化一下” |
| 员工端影响 | 页面、表单、动作、字段 |
| 中心端影响 | 审批、分派、验收、驾驶舱 |
| 技术端影响 | 配置、监控、补偿、审计 |
| 权威主表 | `schema.table` |
| 明细/关系表 | 涉及表 |
| 状态变化 | 当前状态 + 动作 → 下一状态 |
| 权限变化 | RBAC、ABAC、RLS、字段、MFA、回避 |
| 事件变化 | 新增/变更事件及版本 |
| 外部接口 | 幂等、超时、重试、补偿 |
| 数据迁移 | Expand、回填、校验、Contract |
| 安全与隐私 | P0-P3/DATA-L1-L4 影响 |
| 测试 | 单元、集成、E2E、安全、恢复 |
| 回滚 | 代码、配置、数据库和消息回滚 |

---

## 23. 代码审查清单

审查者和智能体必须检查：

### 23.1 业务

- 是否对应正确 `process_code`？
- 是否完成而非只到“已派单/已上传/已通知”？
- 是否有退回、驳回、撤回、取消、超时、部分失败和补偿？
- 是否有客观验收和归档门槛？
- 是否错误自动作出人事、财务、法律、安全结论？

### 23.2 数据

- 是否使用权威主表和现有字段？
- 是否复制了主数据或三端数据？
- 是否使用正确金额、时间、枚举和敏感类型？
- 是否有租户、唯一约束、乐观锁和索引？
- 是否保留历史、版本和审计？

### 23.3 权限

- 后端是否真实鉴权？
- 搜索、导出、附件和批量接口是否同样受控？
- 是否阻断本人审批本人和利益冲突？
- P2/P3 是否脱敏、加密、MFA 和逐次审计？
- 技术角色是否越权读取业务数据？

### 23.4 可靠性

- 写操作是否幂等？
- 外部调用是否有超时、重试、熔断和死信？
- 业务事实、流程轨迹和 Outbox 是否同一事务？
- 重复事件和回调是否防重？
- 是否存在“前端成功、后端失败”？

### 23.5 质量

- 是否复用现有架构而非平行重写？
- 是否有类型、校验、错误码和可观测性？
- 是否有迁移、回退、测试和文档？
- 是否引入无关依赖、无意义重构或巨大复制文件？
- 是否留下 TODO、假按钮、弱密码、测试验证码或敏感数据？

---

### 23.6 前端专项审查

- 是否仍存在跨三个应用复制的同名门户/组件文件？
- 是否把端侧差异做成策略与组合，而不是复制整页或在共享组件堆大量端类型分支？
- SFC、函数、`computed`、嵌套深度和圈复杂度是否满足 16.8？
- 是否存在空 `catch`、只 `showError` 不返回失败、缺少 `finally` 或旧响应覆盖新状态？
- 表单是否只有 HTML `required`，而缺少 Schema 和服务端领域校验？
- 是否仍有裸权限字符串、数据分级字符串、固定年毫秒数、无说明 `slice` 和客户端伪业务编号？
- `portal-session`、`api-client`、权限、竞态、幂等和本次缺陷是否有测试？
- jscpd、死代码、循环依赖、Lint、类型检查和关键 E2E 是否真实通过？

---

## 24. 完成定义（Definition of Done）

一个功能只有同时满足以下条件才算完成：

- 对应明确流程编码和业务目标；
- 三端职责和字段口径一致；
- 真实前后端闭环，不是静态演示；
- 数据持久化到正确权威主表/明细表；
- 状态由服务端状态机迁移；
- 后端 RBAC + ABAC + RLS + 字段权限通过；
- 幂等、并发和重复回调通过；
- 异常、重试、补偿和人工接管可用；
- 审计和 Trace 完整；
- P2/P3 数据安全；
- PC、移动和三端状态一致；
- 单元、集成、E2E、安全和迁移测试通过；
- 性能无明显回退；
- Windows 中文/空格路径可安装启动；
- README、OpenAPI、事件、数据字典、测试矩阵和 CHANGELOG 更新；
- 重复率、复杂度、死代码、循环依赖、Lint、类型检查和质量棘轮通过；
- 可回滚、可复现、可由业务按验收场景验证；
- `DEFECT-S0/S1` 为 0。

---

## 25. 最终不可突破的红线

1. 三端建立重复业务表或重复业务记录；
2. 前端隐藏但 API、搜索、导出或附件可以越权；
3. 技术管理员自动拥有业务审批和敏感数据知悉权；
4. 直接改数据库状态、余额、库存、积分、核销或审计；
5. 已签、已付、已核销、已出库、已归档事实被覆盖或物理删除；
6. 用一个状态字段混淆流程、业务、有效性和归档；
7. 没有幂等导致重复付款、退款、签章、扣库存、计积分、发权限；
8. 未经审核自动盖章、代员工签名或自动作出重大法律/责任结论；
9. 使用个人账号、个人邮箱、个人网盘或公共 AI 处理企业敏感数据；
10. 把 Excel、微信群、截图、Redis 或浏览器存储作为长期唯一事实源；
11. 把审议稿、建议值、测试值硬编码为生产规则；
12. 生产代码保留测试验证码、默认密码、临时演示通道和假成功；
13. 为赶进度跳过迁移、RLS、权限、审计、补偿和测试；
14. 用删除测试、扩大 `any`、关闭规则或忽略错误让 CI 变绿；
15. 未读取对应流程和数据字典就批量生成代码；
16. 为视觉效果牺牲可读性、性能、安全和员工尊严；
17. 未经要求大规模重写现有可运行架构；
18. 声称“全部完成”但没有可运行结果和验证证据；
19. 在员工端、中心端和技术端复制维护同一门户、组件或业务实现，而没有共享权威实现；
20. 通过空 `catch`、只弹提示、关闭静态规则、扩大忽略目录或降低测试断言掩盖真实失败。

---

## 26. 推荐实施顺序

1. 基础设施：三库、角色、Schema、对象存储、Redis、消息、备份和监控；
2. 公共内核：`core/iam/org/mdm/workflow/document/notification/integration/audit`；
3. 公共流程：`P001`—`P020`；
4. 组织、人事、考勤、学习、绩效、通知和财务高频流程；
5. 各中心领域按优先级逐个完成垂直闭环；
6. 子流程稳定后启用 `P120`—`P126` 黄金路径编排；
7. 通过 CDC/ETL 构建 `sjg_dw`，不得阻塞交易上线；
8. 最后进行性能、自动化、移动端和智能辅助优化。

禁止一次性“大爆炸”生成全部模块后再统一修复。

---

## 27. 126 个业务流程权威注册表

> 下表用于代码、测试、日志、事件、迁移和文档追溯。权威 Schema 和主表以数据库包的最新 `01_process_catalog.csv` 和 `07_流程落表映射.csv` 为准。若后续正式变更，必须同步更新本附录或由生成脚本校验。

### 平台公共能力（20 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P001` | 统一登录与多岗位身份切换 | 全员公共能力 | `iam` | `login_session` |
| `P002` | 权限申请、复核与回收 | 全员公共能力 | `iam` | `permission_request` |
| `P003` | 个人资料变更 | 全员公共能力 | `hr` | `employee_profile_change` |
| `P004` | 通用申请与审批 | 全员公共能力 | `workflow` | `generic_request` |
| `P005` | 制度、通知与执行回执 | 全员公共能力 | `collaboration` | `notice` |
| `P006` | 会议与行动项 | 全员公共能力 | `collaboration` | `meeting` |
| `P007` | 排班与班次调整 | 全员公共能力 | `attendance` | `shift_change_request` |
| `P008` | 请假与考勤 | 全员公共能力 | `attendance` | `leave_request` |
| `P009` | 加班与调休 | 全员公共能力 | `attendance` | `overtime_request` |
| `P010` | 员工学习、考试与资格 | 全员公共能力 | `learning` | `learning_assignment` |
| `P011` | 绩效管理 | 全员公共能力 | `performance` | `performance_cycle` |
| `P012` | 晋升与任职发展 | 全员公共能力 | `hr` | `promotion_request` |
| `P013` | 奖励 | 全员公共能力 | `reward` | `reward_case` |
| `P014` | 纪律、责任与申诉 | 全员公共能力 | `reward` | `discipline_case` |
| `P015` | 成长积分与荣誉积分 | 全员公共能力 | `reward` | `point_transaction` |
| `P016` | 员工福利与关怀 | 全员公共能力 | `welfare` | `care_case` |
| `P017` | 电子签署 | 全员公共能力 | `document` | `signature_envelope` |
| `P018` | 数据导入 | 全员公共能力 | `integration` | `data_import_job` |
| `P019` | 敏感导出与文件下载 | 全员公共能力 | `audit` | `data_export_request` |
| `P020` | 数据质量与修复 | 全员公共能力 | `audit` | `data_quality_issue` |

### 总裁办（3 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P021` | 经营会议闭环 | 总裁办经营管理模块 | `collaboration` | `meeting` |
| `P022` | 总裁指令闭环 | 总裁办经营管理模块 | `collaboration` | `executive_directive` |
| `P023` | 重大事项升级 | 总裁办经营管理模块 | `collaboration` | `major_matter` |

### 财人中心（16 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P024` | 预算闭环 | 财务部业务模块 | `finance` | `budget_request` |
| `P025` | 报销闭环 | 财务部业务模块 | `finance` | `expense_claim` |
| `P026` | 借款核销 | 财务部业务模块 | `finance` | `loan_request` |
| `P027` | 跨系统对账 | 财务部业务模块 | `finance` | `reconciliation_task` |
| `P028` | 录用入职闭环 | 人力资源部业务模块 | `hr` | `employee_onboarding` |
| `P029` | 调岗闭环 | 人力资源部业务模块 | `hr` | `employee_transfer` |
| `P030` | 离职闭环 | 人力资源部业务模块 | `hr` | `employee_offboarding` |
| `P031` | 归档接收 | 档案管理部业务模块 | `archive` | `archive_package` |
| `P032` | 借阅归还 | 档案管理部业务模块 | `archive` | `borrow_request` |
| `P033` | 销毁鉴定 | 档案管理部业务模块 | `archive` | `disposal_appraisal` |
| `P034` | 标准采购 | 采购部业务模块 | `procurement` | `purchase_request` |
| `P035` | 紧急采购 | 采购部业务模块 | `procurement` | `emergency_purchase` |
| `P036` | 退换货 | 采购部业务模块 | `procurement` | `purchase_return` |
| `P037` | 售检票闭环 | 票务服务部业务模块 | `ticketing` | `ticket_order` |
| `P038` | 退款改签 | 票务服务部业务模块 | `ticketing` | `ticket_refund_change` |
| `P039` | 离线检票 | 票务服务部业务模块 | `ticketing` | `offline_check_event` |

### 市场中心（6 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P040` | 商机推进 | 市场部业务模块 | `crm` | `opportunity` |
| `P041` | 市场活动 | 市场部业务模块 | `marketing` | `campaign` |
| `P042` | 渠道准入 | 市场部业务模块 | `crm` | `channel_partner` |
| `P043` | 课程产品上线 | 教育部业务模块 | `education` | `course_product` |
| `P044` | 研学团队交付 | 教育部业务模块 | `education` | `study_tour_delivery` |
| `P045` | 讲师排课 | 教育部业务模块 | `education` | `teacher_schedule` |

### 行政中心（16 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P046` | 通知执行 | 办公室综合行政模块 | `collaboration` | `notice` |
| `P047` | 正式公文 | 办公室综合行政模块 | `administration` | `official_document` |
| `P048` | 会议行动 | 办公室综合行政模块 | `collaboration` | `meeting` |
| `P049` | 大型活动 | 办公室综合行政模块 | `administration` | `large_event` |
| `P050` | 入库 | 资产部业务模块 | `asset` | `stock_in` |
| `P051` | 领借调还 | 资产部业务模块 | `asset` | `asset_use_transaction` |
| `P052` | 盘点 | 资产部业务模块 | `asset` | `stocktake` |
| `P053` | 报废处置 | 资产部业务模块 | `asset` | `asset_disposal` |
| `P054` | 派车 | 后勤部业务模块 | `fleet` | `vehicle_dispatch` |
| `P055` | 车辆事故 | 后勤部业务模块 | `fleet` | `vehicle_incident` |
| `P056` | 宿舍入住退宿 | 后勤部业务模块 | `dormitory` | `stay_record` |
| `P057` | 报修 | 后勤部业务模块 | `maintenance` | `repair_order` |
| `P058` | 团餐交付 | 餐饮部业务模块 | `catering` | `group_meal_order` |
| `P059` | 食材验收 | 餐饮部业务模块 | `catering` | `ingredient_acceptance` |
| `P060` | 食品安全事件 | 餐饮部业务模块 | `catering` | `food_safety_incident` |
| `P061` | 餐卡结算 | 餐饮部业务模块 | `catering` | `meal_card_settlement` |

### 物业中心（10 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P062` | 日常巡逻 | 安保部业务模块 | `security` | `patrol_task` |
| `P063` | 客流预警 | 安保部业务模块 | `security` | `crowd_alert` |
| `P064` | 安全事件 | 安保部业务模块 | `security` | `security_incident` |
| `P065` | 活动安保 | 安保部业务模块 | `security` | `event_security_plan` |
| `P066` | 日常清洁 | 环卫部业务模块 | `cleaning` | `cleaning_task` |
| `P067` | 污染应急 | 环卫部业务模块 | `cleaning` | `pollution_incident` |
| `P068` | 活动保障 | 环卫部业务模块 | `cleaning` | `event_support` |
| `P069` | 商户准入 | 商管部业务模块 | `merchant` | `merchant_admission` |
| `P070` | 日常巡检 | 商管部业务模块 | `merchant` | `merchant_inspection` |
| `P071` | 退场 | 商管部业务模块 | `merchant` | `merchant_exit` |

### 演艺中心（13 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P072` | 节目上线 | 表演部业务模块 | `entertainment` | `program` |
| `P073` | 场次执行 | 表演部业务模块 | `entertainment` | `show_session` |
| `P074` | 临时缺员 | 表演部业务模块 | `entertainment` | `staff_shortage` |
| `P075` | 停演 | 表演部业务模块 | `entertainment` | `suspension_decision` |
| `P076` | 场次准备 | 服化道业务模块 | `costume` | `session_preparation` |
| `P077` | 损坏报损 | 服化道业务模块 | `costume` | `damage_report` |
| `P078` | 盘点 | 服化道业务模块 | `costume` | `inventory_check` |
| `P079` | 飞行任务 | 无人机飞行部业务模块 | `drone` | `flight_task` |
| `P080` | 任务取消 | 无人机飞行部业务模块 | `drone` | `flight_cancellation` |
| `P081` | 飞行事故 | 无人机飞行部业务模块 | `drone` | `flight_incident` |
| `P082` | 场次技术保障 | 演艺技术部业务模块 | `entertainment` | `tech_support_task` |
| `P083` | 设备故障 | 演艺技术部业务模块 | `entertainment` | `equipment_fault` |
| `P084` | 停演技术建议 | 演艺技术部业务模块 | `entertainment` | `suspension_advice` |

### 网络中心（10 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P085` | 网络故障 | 网络技术部业务模块 | `itops` | `network_incident` |
| `P086` | 网络变更 | 网络技术部业务模块 | `itops` | `network_change` |
| `P087` | 活动保障 | 网络技术部业务模块 | `itops` | `event_support` |
| `P088` | IT服务请求 | 系统维护部业务模块 | `itops` | `service_request` |
| `P089` | 生产发布 | 系统维护部业务模块 | `devops` | `release` |
| `P090` | 账号执行 | 系统维护部业务模块 | `iam` | `account_execution` |
| `P091` | 恢复演练 | 系统维护部业务模块 | `itops` | `recovery_drill` |
| `P092` | 需求开发 | 研发部业务模块 | `devops` | `requirement` |
| `P093` | 缺陷修复 | 研发部业务模块 | `devops` | `defect` |
| `P094` | 版本发布 | 研发部业务模块 | `devops` | `release` |

### 企划中心（6 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P095` | 宣传物料制作 | 企宣部业务模块 | `content` | `material_request` |
| `P096` | 拍摄任务 | 企宣部业务模块 | `content` | `shooting_task` |
| `P097` | 返工变更 | 企宣部业务模块 | `content` | `change_request` |
| `P098` | 策划项目 | 策划部业务模块 | `planning` | `project` |
| `P099` | 方案变更 | 策划部业务模块 | `planning` | `change_request` |
| `P100` | 活动策划移交 | 策划部业务模块 | `planning` | `event_handover` |

### 新媒体中心（11 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P101` | 产品上架 | OTA部业务模块 | `ecommerce` | `product_listing` |
| `P102` | OTA订单 | OTA部业务模块 | `ecommerce` | `ota_order` |
| `P103` | 退款 | OTA部业务模块 | `ecommerce` | `refund_order` |
| `P104` | 评价客诉 | OTA部业务模块 | `ecommerce` | `review_complaint` |
| `P105` | 短视频发布 | 抖音部业务模块 | `media` | `short_video_release` |
| `P106` | 直播执行 | 抖音部业务模块 | `media` | `live_session` |
| `P107` | 投放 | 抖音部业务模块 | `media` | `ad_campaign` |
| `P108` | 舆情 | 抖音部业务模块 | `media` | `public_opinion_case` |
| `P109` | 品牌授权 | 品牌部业务模块 | `brand` | `authorization` |
| `P110` | 物料审核 | 品牌部业务模块 | `brand` | `material_review` |
| `P111` | 侵权处理 | 品牌部业务模块 | `brand` | `infringement_case` |

### 运营中心（8 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P112` | 标准接待 | 接待部业务模块 | `reception` | `reception_order` |
| `P113` | 临时变更 | 接待部业务模块 | `reception` | `change_request` |
| `P114` | 导游执行 | 接待部业务模块 | `reception` | `guide_execution` |
| `P115` | 接待客诉 | 接待部业务模块 | `reception` | `complaint` |
| `P116` | 线索转商机 | 营销部业务模块 | `sales` | `lead` |
| `P117` | 销售成交 | 营销部业务模块 | `sales` | `deal` |
| `P118` | 二次销售 | 营销部业务模块 | `sales` | `secondary_sale` |
| `P119` | 客户移交 | 营销部业务模块 | `sales` | `customer_handover` |

### 跨中心协同（7 个流程）
| 流程编码 | 流程名称 | 业务模块 | 权威 Schema | 权威主表 |
|---|---|---|---|---|
| `P120` | 全员生命周期闭环 | 黄金路径 | `workflow` | `wf_orchestration_instance` |
| `P121` | 市场获客到交付回款闭环 | 黄金路径 | `workflow` | `wf_orchestration_instance` |
| `P122` | 接待资源闭环 | 黄金路径 | `workflow` | `wf_orchestration_instance` |
| `P123` | 演艺场次闭环 | 黄金路径 | `workflow` | `wf_orchestration_instance` |
| `P124` | 采购资产财务闭环 | 黄金路径 | `workflow` | `wf_orchestration_instance` |
| `P125` | 内容生产发布闭环 | 黄金路径 | `workflow` | `wf_orchestration_instance` |
| `P126` | 事件与整改闭环 | 黄金路径 | `workflow` | `wf_orchestration_instance` |

---

## 28. 智能体提交前自检问题

### 28.1 编码前

1. 服务的是哪个端、哪个角色、哪个流程编码？
2. 数据唯一来源和权威主表是什么？
3. 当前状态、允许动作和下一状态从哪里定义？
4. 谁申请、谁复核、谁批准、谁执行、谁验收？
5. 是否存在本人审批本人或利益冲突？
6. 需要哪些前置条件和客观证据？
7. 成功、退回、驳回、撤回、取消、超时和部分失败如何处理？
8. 是否需要幂等、锁、版本、冲销和补偿？
9. 涉及哪些 P2/P3 字段，当前角色真的需要看到吗？
10. 是否能够少收一个字段、少复制一份数据、少创建一张表？
11. 是否错误地把审议建议值当生产规则？
12. 是否有可测试、可回滚、可审计的实现方案？

### 28.2 编码后

1. 前端显示成功时，事务、流程动作和 Outbox 是否全部成功？
2. 刷新、重复点击、跨端、跨设备后是否一致？
3. 直接改 URL、请求参数、租户 ID、员工 ID 能否越权？
4. 技术管理员是否能看到不该看的业务数据？
5. 日志、Trace、错误和事件是否泄露敏感信息？
6. 外部失败是否会重复付款、签章、短信、库存或权限？
7. 归档和不可逆事实是否仍可被普通编辑接口修改？
8. 数据库迁移是否从空库和上一版本均验证？
9. Windows 中文路径是否实际运行？
10. 文档、OpenAPI、数据字典、测试和变更记录是否同步？

---


### 29.1 强制整改顺序

| 优先级 | 整改内容 | 合并门槛 |
|---|---|---|
| P0 | 消除三端整页/整文件复制 | 不再存在三份完全相同门户；共享能力只有一个权威实现 |
| P0 | 拆分巨型 `CenterPortal.vue` | 页面壳满足 16.8，功能区块可独立测试，行为与权限不回退 |
| P1 | 统一 API Client、异步状态、错误边界和竞态处理 | 无空 `catch`；无“只 showError”；旧请求不能覆盖新状态 |
| P1 | 统一 Schema 校验、常量、权限码、日期和 ID 规则 | 关键表单有双层校验；无固定年毫秒和客户端伪业务 ID |
| P1 | 后端权限与幂等闭环 | 每个关键写 API 二次鉴权并验证 `Idempotency-Key` |
| P2 | 补齐会话、API、门户和缺陷回归测试 | 关键分支达到 19.5；CI 全部门禁通过 |
| P2 | 清理死代码、废弃状态和生产 `console` | 死代码工具无未批准高风险结果，构建产物干净 |

### 29.2 整改期间的发布限制

- 在 P0 完成前，可以修复安全、数据损坏和阻断性缺陷，但禁止继续复制门户文件或向巨型 SFC 填入新业务板块。
- 新功能必须先进入共享架构和正确领域边界，不能以“先复制以后再改”为理由增加技术债。
- 任何重构必须有行为保护测试、变更前后质量数字、启动验证和明确回滚方式。
- 不允许把“架构级重构”理解为全仓库重写；必须按 21.7 渐进替换，持续保持可运行。
- 专项整改完成的客观标准：跨端精确复制为 0、全仓库重复率低于 15%、新改代码低于 5%、质量门禁全部启用、P0/P1 缺陷为 0、关键流程回归测试通过。

---

## 30. 一句话准则

> **把复杂留给系统，把清楚和下一步留给员工；把每一次操作做成有身份、有权限、有依据、有证据、有验收、有补偿、可追溯的真实闭环。**
