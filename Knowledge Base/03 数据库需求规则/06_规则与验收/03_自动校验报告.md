# 数据库架构自动校验报告

校验日期：2026-08-06 18:10:22 +0800  
版本：V1.0

## 1. 规模校验

| 项目 | 数量 | 结果 |
|---|---:|---|
| 核心数据库 | 3 | 通过 |
| 物理 Schema | 46 | 通过 |
| 物理表 | 265 | 通过 |
| 字段定义 | 7,116 | 通过 |
| 主外键关系 | 1,335 | 通过 |
| 索引与唯一约束 | 1,024 | 通过 |
| 业务流程 | 126 | 通过 |
| 原始字段映射 | 90,124 | 通过 |
| 唯一源字段 | 445 | 通过 |
| SQL 文件 | 60 | 通过 |

## 2. 结构一致性

- 物理表名称全局唯一：通过。
- 同一表内字段名称无重复：通过。
- 表清单字段数与字段字典实际行数一致：通过。
- 1,335 条关系的源表、源字段、目标表、目标字段均存在：通过。
- 1,024 条索引的目标表和索引字段均存在，Schema 内索引名无重复：通过。
- 126 个流程编码唯一，且每个流程均存在唯一权威主表映射：通过。
- 流程映射中的主表、明细表、附件表均存在：通过。

## 3. 字段追溯校验

- 90,124 条原始字段映射均保留流程、端口、表单、字段标识、目标库、目标表、承载列和转换规则：通过。
- 公共流程字段已统一映射到流程内核真实列或明确 JSON 路径：通过。
- 领域稳定字段直接落类型化列；重复项落明细表；附件落文件服务；关系落关系表：通过。
- `signing_order` 已统一落 `document.signature_party.sign_order`，并增加正整数校验和信封内顺序唯一约束：通过。

## 4. DDL 校验

- 数据字典中的 265 张物理表均存在对应 `CREATE TABLE IF NOT EXISTS`：通过。
- DDL 表数量与数据字典表数量一致：通过。
- SQL 文件中未发现 `TODO`、`TBD`、`FIXME` 或 `PLACEHOLDER`：通过。
- RLS、触发器、外键、索引、视图、流程目录及分区模板均已生成：通过。

## 5. Excel 校验

- `上金谷数据库架构与全量数据字典.xlsx`：10 个工作表，压缩结构完整。
- `上金谷流程状态规则接口初始化数据.xlsx`：6 个工作表，压缩结构完整。
- 两份工作簿公式错误扫描：未发现 `#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?`、`#N/A`。

## 6. 结论

**最终结构错误：0**  
**最终映射错误：0**  
**最终警告：0**

本交付包可作为数据库详细设计、研发建库、接口联调、数据迁移和上线验收的基线。生产上线前仍需结合实际 PostgreSQL 小版本、部署拓扑、数据规模、合规保留期及第三方接口完成环境评审。
